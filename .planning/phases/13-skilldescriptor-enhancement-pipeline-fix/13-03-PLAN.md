---
phase: 13-skilldescriptor-enhancement-pipeline-fix
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - api/app/agent/skills/extract-characters/SKILL.md
  - api/app/agent/skills/extract-scenes/SKILL.md
  - api/app/agent/skills/refine-text/SKILL.md
  - api/app/agent/skills/split-clips/SKILL.md
  - api/app/agent/skills/episode-pipeline/SKILL.md
  - api/tests/test_skill_annotations.py
autonomous: true
requirements:
  - DESC-05
gap_closure: true

must_haves:
  truths:
    - "extract-characters, extract-scenes, refine-text, split-clips SKILL.md files have is_read_only: true"
    - "episode-pipeline SKILL.md has is_destructive: true"
    - "Runtime system prompt from build_system_prompt_fragment() contains [只读] labels"
    - "Runtime system prompt from build_system_prompt_fragment() contains [⚠️ 破坏性操作] label"
    - "A test asserts safety annotations appear in the built system prompt"
  artifacts:
    - path: "api/app/agent/skills/extract-characters/SKILL.md"
      provides: "Read-only safety annotation"
      contains: "is_read_only: true"
    - path: "api/app/agent/skills/episode-pipeline/SKILL.md"
      provides: "Destructive safety annotation"
      contains: "is_destructive: true"
    - path: "api/tests/test_skill_annotations.py"
      provides: "Runtime prompt safety label assertion"
      contains: "test_system_prompt_contains_safety_annotations"
  key_links:
    - from: "api/app/agent/skills/*/SKILL.md"
      to: "api/app/agent/skill_loader.py"
      via: "_parse_frontmatter() reads is_read_only/is_destructive → build_system_prompt_fragment() renders labels"
      pattern: "is_read_only.*True|\\[只读\\]"
---

<objective>
Close the single verification gap from Phase 13: safety annotations never appear in the runtime system prompt because all 10 SKILL.md files set both `is_read_only: false` and `is_destructive: false`.

Purpose: Make the safety metadata system behaviorally complete — the `build_system_prompt_fragment()` code already renders `[只读]` and `[⚠️ 破坏性操作]` labels, but no skill triggers them.
Output: 5 SKILL.md files with semantically correct safety values; 1 runtime test proving annotations appear.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-skilldescriptor-enhancement-pipeline-fix/13-CONTEXT.md
@.planning/phases/13-skilldescriptor-enhancement-pipeline-fix/13-VERIFICATION.md

<interfaces>
From api/app/agent/skill_loader.py (lines 100-117 — already renders safety labels, no code change needed):
```python
def build_system_prompt_fragment(self) -> str:
    with self._lock:
        if not self._metadata:
            return ""
        lines = [
            "## 可用技能\n",
            "使用 `read_skill(name)` 获取技能详细指令后再执行。\n",
        ]
        for m in self._metadata.values():
            safety = ""
            if m.is_read_only:
                safety += " [只读]"
            if m.is_destructive:
                safety += " [⚠️ 破坏性操作]"
            tier_label = f" [{m.skill_tier}]" if m.skill_tier != "capability" else ""
            lines.append(f"- **{m.name}**{tier_label}: {m.description}{safety}")
        return "\n".join(lines)
```

From api/tests/test_skill_annotations.py (existing test file — add new test here):
```python
from app.agent.tools import TOOL_METADATA, get_all_tools

SKILLS_ROOT = Path(__file__).resolve().parents[1] / "app" / "agent" / "skills"
REQUIRED_FIELDS = { "skill_kind", "skill_tier", "is_read_only", "is_destructive", "timeout", "max_result_size_chars" }
```

Current SKILL.md frontmatter pattern (extract-characters as example — all 10 skills follow this layout):
  name: extract-characters
  skill_kind: extract_characters
  skill_tier: capability
  is_read_only: false    ← change to true for read-only skills
  is_destructive: false  ← change to true for episode-pipeline
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set semantically correct safety metadata on 5 SKILL.md files</name>
  <files>api/app/agent/skills/extract-characters/SKILL.md, api/app/agent/skills/extract-scenes/SKILL.md, api/app/agent/skills/refine-text/SKILL.md, api/app/agent/skills/split-clips/SKILL.md, api/app/agent/skills/episode-pipeline/SKILL.md</files>
  <read_first>
    - api/app/agent/skills/extract-characters/SKILL.md
    - api/app/agent/skills/extract-scenes/SKILL.md
    - api/app/agent/skills/refine-text/SKILL.md
    - api/app/agent/skills/split-clips/SKILL.md
    - api/app/agent/skills/episode-pipeline/SKILL.md
    - api/app/agent/skill_loader.py (lines 100-117, confirm rendering logic)
  </read_first>
  <action>
Change the `is_read_only` field from `false` to `true` in these 4 SKILL.md files:

1. **api/app/agent/skills/extract-characters/SKILL.md** — Change `is_read_only: false` → `is_read_only: true`
   Rationale: reads script, returns structured character data; no project state mutation.

2. **api/app/agent/skills/extract-scenes/SKILL.md** — Change `is_read_only: false` → `is_read_only: true`
   Rationale: reads script, returns structured scene data; no project state mutation.

3. **api/app/agent/skills/refine-text/SKILL.md** — Change `is_read_only: false` → `is_read_only: true`
   Rationale: pure text transformation with no persistent side effects.

4. **api/app/agent/skills/split-clips/SKILL.md** — Change `is_read_only: false` → `is_read_only: true`
   Rationale: splits text into segments, no state mutation.

Change the `is_destructive` field from `false` to `true` in this 1 SKILL.md file:

5. **api/app/agent/skills/episode-pipeline/SKILL.md** — Change `is_destructive: false` → `is_destructive: true`
   Rationale: orchestrates full chain (split→convert→storyboard→detail→generate), creates/overwrites project data irreversibly.

Do NOT change any other fields. Do NOT modify `skill_loader.py` — the rendering code already handles these flags correctly.
  </action>
  <verify>
    <automated>cd api && grep 'is_read_only: true' app/agent/skills/extract-characters/SKILL.md app/agent/skills/extract-scenes/SKILL.md app/agent/skills/refine-text/SKILL.md app/agent/skills/split-clips/SKILL.md && grep 'is_destructive: true' app/agent/skills/episode-pipeline/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `api/app/agent/skills/extract-characters/SKILL.md` contains exactly `is_read_only: true`
    - `api/app/agent/skills/extract-scenes/SKILL.md` contains exactly `is_read_only: true`
    - `api/app/agent/skills/refine-text/SKILL.md` contains exactly `is_read_only: true`
    - `api/app/agent/skills/split-clips/SKILL.md` contains exactly `is_read_only: true`
    - `api/app/agent/skills/episode-pipeline/SKILL.md` contains exactly `is_destructive: true`
    - All 5 files still contain `is_read_only:` and `is_destructive:` fields (no accidental deletion)
    - The other 5 SKILL.md files are NOT modified (convert-screenplay, create-storyboard, detail-storyboard, generate-shot-image, generate-shot-video remain is_read_only: false, is_destructive: false)
  </acceptance_criteria>
  <done>4 read-only skills marked is_read_only: true; 1 workflow skill marked is_destructive: true; remaining 5 skills unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add runtime prompt safety annotation test</name>
  <files>api/tests/test_skill_annotations.py</files>
  <read_first>
    - api/tests/test_skill_annotations.py
    - api/app/agent/skill_loader.py (lines 1-30 for SkillMeta, lines 100-117 for build_system_prompt_fragment)
  </read_first>
  <action>
Add a new test function `test_system_prompt_contains_safety_annotations` to the existing `api/tests/test_skill_annotations.py` file.

The test must:
1. Import `SkillLoader` from `app.agent.skill_loader`
2. Instantiate a `SkillLoader()` and call `load_metadata()` to populate the internal cache from the real SKILL.md files
3. Call `build_system_prompt_fragment()` to get the rendered prompt string
4. Assert the prompt contains `[只读]` at least once (from the 4 read-only skills)
5. Assert the prompt contains `[⚠️ 破坏性操作]` at least once (from episode-pipeline)
6. Assert the prompt contains `extract-characters` (sanity check that skills are loaded)

Additionally, add a second test `test_read_only_skills_have_correct_count` that:
1. Loads all 10 SKILL.md frontmatters using the existing `_skill_metadata()` helper
2. Counts skills where `is_read_only` is `True` — asserts exactly 4
3. Counts skills where `is_destructive` is `True` — asserts exactly 1
4. Asserts the read-only skills are exactly: extract-characters, extract-scenes, refine-text, split-clips
5. Asserts the destructive skill is exactly: episode-pipeline

Add these two functions at the end of the file, after the existing `test_meta_tools_are_read_only` function.
  </action>
  <verify>
    <automated>cd api && uv run pytest tests/test_skill_annotations.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `test_skill_annotations.py` contains function `test_system_prompt_contains_safety_annotations`
    - `test_skill_annotations.py` contains function `test_read_only_skills_have_correct_count`
    - The prompt test asserts `[只读]` appears in `build_system_prompt_fragment()` output
    - The prompt test asserts `[⚠️ 破坏性操作]` appears in `build_system_prompt_fragment()` output
    - The count test asserts exactly 4 read-only skills and 1 destructive skill
    - `cd api && uv run pytest tests/test_skill_annotations.py -x` exits 0
    - `cd api && uv run pytest -x -q` exits 0 (no regression)
  </acceptance_criteria>
  <done>Two new tests pass: runtime prompt contains both [只读] and [⚠️ 破坏性操作] labels; safety metadata counts are correct (4 read-only, 1 destructive).</done>
</task>

</tasks>

<verification>
- `cd api && uv run pytest tests/test_skill_annotations.py -x -v` — all tests green including 2 new safety tests
- `cd api && uv run pytest tests/test_descriptor_fields.py tests/test_skill_registration.py tests/test_skill_annotations.py tests/test_tool_middleware.py -x -v` — full Phase 13 test suite green
- `cd api && uv run pytest -x -q` — full backend test suite green (no regressions)
- `cd api && uv run python -c "from app.agent.skill_loader import SkillLoader; sl = SkillLoader(); sl.load_metadata(); p = sl.build_system_prompt_fragment(); assert '[只读]' in p; assert '[⚠️ 破坏性操作]' in p; print('PASS: safety annotations present')"` — prints PASS
</verification>

<success_criteria>
1. 4 read-only skills (extract-characters, extract-scenes, refine-text, split-clips) marked `is_read_only: true`
2. 1 destructive skill (episode-pipeline) marked `is_destructive: true`
3. Runtime `build_system_prompt_fragment()` output contains `[只读]` and `[⚠️ 破坏性操作]` labels
4. New tests validate both safety metadata values and runtime prompt content
5. Full backend test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/13-skilldescriptor-enhancement-pipeline-fix/13-03-SUMMARY.md`
</output>
