---
phase: 13-skilldescriptor-enhancement-pipeline-fix
plan: 02
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - api/app/agent/skills/extract-characters/SKILL.md
  - api/app/agent/skills/extract-scenes/SKILL.md
  - api/app/agent/skills/split-clips/SKILL.md
  - api/app/agent/skills/convert-screenplay/SKILL.md
  - api/app/agent/skills/create-storyboard/SKILL.md
  - api/app/agent/skills/detail-storyboard/SKILL.md
  - api/app/agent/skills/generate-shot-image/SKILL.md
  - api/app/agent/skills/generate-shot-video/SKILL.md
  - api/app/agent/skills/refine-text/SKILL.md
  - api/app/agent/skills/episode-pipeline/SKILL.md
  - api/app/agent/tools/__init__.py
  - api/app/agent/tool_middleware.py
  - api/tests/test_skill_annotations.py
  - api/tests/test_tool_middleware.py
autonomous: true
requirements:
  - DESC-07
  - DESC-08
must_haves:
  truths:
    - "All 10 SKILL.md files have complete YAML frontmatter with skill_kind, skill_tier, require_prior_kind, supports_skip, is_read_only, is_destructive, timeout, max_result_size_chars"
    - "All 17 @tool functions have metadata dicts attached with at least skill_tier and context_group fields"
    - "get_tools_for_context() uses tool.metadata instead of hardcoded name sets"
    - "Tool filtering still produces at most 14 tools per context window"
    - "Existing tool sets per context scenario are preserved (same tools exposed before and after refactor)"
  artifacts:
    - path: "api/app/agent/skills/extract-characters/SKILL.md"
      provides: "SKILL.md with complete Phase 13 frontmatter"
      contains: "skill_kind: extract_characters"
    - path: "api/app/agent/tools/__init__.py"
      provides: "TOOL_METADATA dict + attach_tool_metadata() + updated get_all_tools()"
      contains: "TOOL_METADATA"
    - path: "api/app/agent/tool_middleware.py"
      provides: "Metadata-driven get_tools_for_context()"
      contains: "metadata"
    - path: "api/tests/test_skill_annotations.py"
      provides: "Integration tests verifying all 14 skills annotated"
      contains: "test_all_skill_md_have_frontmatter"
    - path: "api/tests/test_tool_middleware.py"
      provides: "Tests for metadata-driven filtering scenarios"
      contains: "test_default_context"
  key_links:
    - from: "api/app/agent/tools/__init__.py"
      to: "api/app/agent/tool_middleware.py"
      via: "get_all_tools() attaches metadata, get_tools_for_context() reads it"
      pattern: "attach_tool_metadata"
    - from: "api/app/agent/tool_middleware.py"
      to: "tool.metadata"
      via: "getattr(t, metadata, None) or {} to read skill_tier, context_group"
      pattern: "meta.get"
---

<objective>
Annotate all 14 skills (10 SKILL.md frontmatter + 4 @tool metadata) with Phase 13 descriptor fields. Create TOOL_METADATA dict for all 17 @tools. Refactor tool_middleware from hardcoded name sets to metadata-driven dynamic filtering.

Purpose: Complete the skill metadata system (DESC-07) and make tool filtering maintainable and extensible (DESC-08).
Output: Fully annotated skills, metadata-driven filtering, comprehensive test coverage.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-skilldescriptor-enhancement-pipeline-fix/13-CONTEXT.md
@.planning/phases/13-skilldescriptor-enhancement-pipeline-fix/13-RESEARCH.md
@.planning/phases/13-skilldescriptor-enhancement-pipeline-fix/13-01-SUMMARY.md

<interfaces>
From api/app/agent/tools/__init__.py (current, extend this):
```python
def get_all_tools() -> list:
    from app.agent.tools.context_tools import CONTEXT_TOOLS
    from app.agent.tools.mutation_tools import MUTATION_TOOLS
    from app.agent.tools.ai_tools import AI_TOOLS
    from app.agent.tools.skill_tools import SKILL_TOOLS
    return [*CONTEXT_TOOLS, *MUTATION_TOOLS, *AI_TOOLS, *SKILL_TOOLS]

def get_tools_for_context(*, has_canvas: bool = False, has_episode: bool = False) -> list:
    from app.agent.tool_middleware import get_tools_for_context as _gate
    return _gate(get_all_tools(), has_canvas=has_canvas, has_episode=has_episode)
```

From api/app/agent/tool_middleware.py (current hardcoded sets, replace with metadata):
```python
_ALWAYS_TOOLS = {
    "get_project_info", "get_episodes", "get_script",
    "get_characters", "get_scenes",
    "read_skill", "read_resource",
}
_CANVAS_TOOLS = {"get_canvas_state"}
_STORYBOARD_TOOLS = {
    "save_shot_plan", "save_shot_details", "update_shot",
    "generate_image", "generate_video",
    "get_style_templates",
}
_SCRIPT_TOOLS = {
    "save_characters", "save_scenes", "save_screenplay",
}
```

All 17 tool names (from context_tools.py, mutation_tools.py, ai_tools.py, skill_tools.py):
- Context (7): get_characters, get_scenes, get_script, get_canvas_state, get_project_info, get_episodes, get_style_templates
- Mutation (6): save_characters, save_scenes, save_screenplay, save_shot_plan, save_shot_details, update_shot
- AI (2): generate_image, generate_video
- Skill (2): read_skill, read_resource
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate 10 SKILL.md + Define TOOL_METADATA + attach_tool_metadata()</name>
  <files>api/app/agent/skills/split-clips/SKILL.md, api/app/agent/skills/convert-screenplay/SKILL.md, api/app/agent/skills/extract-characters/SKILL.md, api/app/agent/skills/extract-scenes/SKILL.md, api/app/agent/skills/create-storyboard/SKILL.md, api/app/agent/skills/detail-storyboard/SKILL.md, api/app/agent/skills/generate-shot-image/SKILL.md, api/app/agent/skills/generate-shot-video/SKILL.md, api/app/agent/skills/refine-text/SKILL.md, api/app/agent/skills/episode-pipeline/SKILL.md, api/app/agent/tools/__init__.py, api/tests/test_skill_annotations.py</files>
  <read_first>
    - api/app/agent/skills/split-clips/SKILL.md
    - api/app/agent/skills/convert-screenplay/SKILL.md
    - api/app/agent/skills/extract-characters/SKILL.md
    - api/app/agent/skills/extract-scenes/SKILL.md
    - api/app/agent/skills/create-storyboard/SKILL.md
    - api/app/agent/skills/detail-storyboard/SKILL.md
    - api/app/agent/skills/generate-shot-image/SKILL.md
    - api/app/agent/skills/generate-shot-video/SKILL.md
    - api/app/agent/skills/refine-text/SKILL.md
    - api/app/agent/skills/episode-pipeline/SKILL.md
    - api/app/agent/tools/__init__.py
    - api/app/agent/tools/ai_tools.py
    - api/app/agent/tools/skill_tools.py
    - api/app/agent/tools/context_tools.py
    - api/app/agent/tools/mutation_tools.py
    - .planning/phases/13-skilldescriptor-enhancement-pipeline-fix/13-RESEARCH.md
  </read_first>
  <action>
**1. Update each SKILL.md YAML frontmatter** (per D-03, D-13, D-14).

For each SKILL.md, add the following new YAML fields to the existing frontmatter block (between the existing `---` delimiters), keeping the existing `name` and `description` fields unchanged. Add ALL of these fields after the `description` field:

Exact field values per skill (from research annotation map):

split-clips: skill_kind=split_clips, skill_tier=capability, require_prior_kind=[], default_require_prior_kind=[], supports_skip=false, is_read_only=false, is_destructive=false, timeout=120, max_result_size_chars=50000

convert-screenplay: skill_kind=convert_screenplay, skill_tier=capability, require_prior_kind=[split_clips], default_require_prior_kind=[], supports_skip=false, is_read_only=false, is_destructive=false, timeout=120, max_result_size_chars=50000

extract-characters: skill_kind=extract_characters, skill_tier=capability, require_prior_kind=[], default_require_prior_kind=[], supports_skip=true, is_read_only=false, is_destructive=false, timeout=120, max_result_size_chars=50000

extract-scenes: skill_kind=extract_scenes, skill_tier=capability, require_prior_kind=[], default_require_prior_kind=[], supports_skip=true, is_read_only=false, is_destructive=false, timeout=120, max_result_size_chars=50000

create-storyboard: skill_kind=create_storyboard, skill_tier=capability, require_prior_kind=[convert_screenplay], default_require_prior_kind=[], supports_skip=false, is_read_only=false, is_destructive=false, timeout=120, max_result_size_chars=50000

detail-storyboard: skill_kind=detail_storyboard, skill_tier=capability, require_prior_kind=[create_storyboard], default_require_prior_kind=[], supports_skip=true, is_read_only=false, is_destructive=false, timeout=120, max_result_size_chars=50000

generate-shot-image: skill_kind=generate_shot_image, skill_tier=capability, require_prior_kind=[create_storyboard], default_require_prior_kind=[], supports_skip=true, is_read_only=false, is_destructive=false, timeout=120, max_result_size_chars=5000

generate-shot-video: skill_kind=generate_shot_video, skill_tier=capability, require_prior_kind=[generate_shot_image], default_require_prior_kind=[], supports_skip=true, is_read_only=false, is_destructive=false, timeout=120, max_result_size_chars=5000

refine-text: skill_kind=refine_text, skill_tier=capability, require_prior_kind=[], default_require_prior_kind=[], supports_skip=false, is_read_only=false, is_destructive=false, timeout=60, max_result_size_chars=50000

episode-pipeline: skill_kind=episode_pipeline, skill_tier=workflow, require_prior_kind=[], default_require_prior_kind=[], supports_skip=false, is_read_only=false, is_destructive=false, timeout=300, max_result_size_chars=50000

Use YAML list syntax for require_prior_kind when non-empty (e.g., `require_prior_kind:\n  - split_clips`). Use `require_prior_kind: []` when empty.

**2. Define TOOL_METADATA and _attach_tool_metadata() in `api/app/agent/tools/__init__.py`:**

Add at the top of the file (after the docstring), a `TOOL_METADATA` dict mapping all 17 tool names to their metadata. Each entry has keys: `skill_tier`, `skill_kind`, `is_read_only`, `is_destructive`, `timeout`, `max_result_size_chars`, `context_group`.

Context_group values determine filtering behavior:
- "always" — always included (maps to old _ALWAYS_TOOLS)
- "canvas" — included when has_canvas=True (maps to old _CANVAS_TOOLS)
- "episode" — included when has_episode=True (maps to old _STORYBOARD_TOOLS)
- "script" — included when has_episode=False (maps to old _SCRIPT_TOOLS)

Exact TOOL_METADATA entries (17 total):

get_project_info:    skill_tier=meta, skill_kind=get_project_info,    is_read_only=True, is_destructive=False, timeout=60, max_result_size_chars=50000, context_group=always
get_episodes:        skill_tier=meta, skill_kind=get_episodes,        is_read_only=True, is_destructive=False, timeout=60, max_result_size_chars=50000, context_group=always
get_characters:      skill_tier=meta, skill_kind=get_characters,      is_read_only=True, is_destructive=False, timeout=60, max_result_size_chars=50000, context_group=always
get_scenes:          skill_tier=meta, skill_kind=get_scenes,          is_read_only=True, is_destructive=False, timeout=60, max_result_size_chars=50000, context_group=always
get_script:          skill_tier=meta, skill_kind=get_script,          is_read_only=True, is_destructive=False, timeout=60, max_result_size_chars=50000, context_group=always
get_canvas_state:    skill_tier=meta, skill_kind=get_canvas_state,    is_read_only=True, is_destructive=False, timeout=60, max_result_size_chars=50000, context_group=canvas
get_style_templates: skill_tier=meta, skill_kind=get_style_templates, is_read_only=True, is_destructive=False, timeout=60, max_result_size_chars=50000, context_group=episode
save_characters:     skill_tier=capability, skill_kind=save_characters,    is_read_only=False, is_destructive=False, timeout=60, max_result_size_chars=50000, context_group=script
save_scenes:         skill_tier=capability, skill_kind=save_scenes,        is_read_only=False, is_destructive=False, timeout=60, max_result_size_chars=50000, context_group=script
save_screenplay:     skill_tier=capability, skill_kind=save_screenplay,    is_read_only=False, is_destructive=True,  timeout=60, max_result_size_chars=50000, context_group=script
save_shot_plan:      skill_tier=capability, skill_kind=save_shot_plan,     is_read_only=False, is_destructive=True,  timeout=60, max_result_size_chars=50000, context_group=episode
save_shot_details:   skill_tier=capability, skill_kind=save_shot_details,  is_read_only=False, is_destructive=False, timeout=60, max_result_size_chars=50000, context_group=episode
update_shot:         skill_tier=capability, skill_kind=update_shot,        is_read_only=False, is_destructive=False, timeout=60, max_result_size_chars=50000, context_group=episode
generate_image:      skill_tier=capability, skill_kind=generate_image,     is_read_only=False, is_destructive=False, timeout=120, max_result_size_chars=5000, context_group=episode
generate_video:      skill_tier=capability, skill_kind=generate_video,     is_read_only=False, is_destructive=False, timeout=120, max_result_size_chars=5000, context_group=episode
read_skill:          skill_tier=meta, skill_kind=read_skill,         is_read_only=True, is_destructive=False, timeout=10, max_result_size_chars=100000, context_group=always
read_resource:       skill_tier=meta, skill_kind=read_resource,      is_read_only=True, is_destructive=False, timeout=10, max_result_size_chars=100000, context_group=always

Add helper function:
```python
def _attach_tool_metadata(tools: list) -> list:
    """Attach Phase 13 metadata dicts to LangChain StructuredTool objects."""
    for t in tools:
        meta = TOOL_METADATA.get(t.name)
        if meta:
            t.metadata = {**(t.metadata or {}), **meta}
    return tools
```

Update get_all_tools() to call _attach_tool_metadata(tools) before returning.

**3. Create `api/tests/test_skill_annotations.py`** with these tests:

- test_all_skill_md_have_frontmatter() — Scan all 10 SKILL.md files in api/app/agent/skills/*/SKILL.md, parse YAML frontmatter with yaml.safe_load, assert each has skill_kind, skill_tier, is_read_only, is_destructive, timeout, max_result_size_chars.
- test_skill_md_skill_kind_values() — Assert known mappings: extract-characters has skill_kind extract_characters, episode-pipeline has skill_kind episode_pipeline, split-clips has skill_kind split_clips.
- test_skill_md_episode_pipeline_is_workflow() — Assert episode-pipeline has skill_tier=workflow (only workflow skill).
- test_all_tools_have_metadata() — Call get_all_tools(), assert all 17 tools have metadata dict with skill_tier and context_group keys.
- test_tool_metadata_coverage() — Assert len(TOOL_METADATA) == 17.
- test_meta_tools_are_read_only() — For all tools in TOOL_METADATA with skill_tier==meta, assert is_read_only==True.
  </action>
  <verify>
    <automated>cd api && uv run pytest tests/test_skill_annotations.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - All 10 SKILL.md files contain `skill_kind:` in YAML frontmatter
    - All 10 SKILL.md files contain `skill_tier:` in YAML frontmatter
    - episode-pipeline/SKILL.md contains `skill_tier: workflow`
    - convert-screenplay/SKILL.md contains `require_prior_kind:` with `split_clips`
    - tools/__init__.py contains `TOOL_METADATA` dict with 17 entries
    - tools/__init__.py contains `def _attach_tool_metadata`
    - tools/__init__.py get_all_tools() calls `_attach_tool_metadata`
    - test_skill_annotations.py contains at least 5 test functions
    - `uv run pytest tests/test_skill_annotations.py -x` exits 0
  </acceptance_criteria>
  <done>All 10 SKILL.md files have complete Phase 13 frontmatter. All 17 @tools have metadata dicts attached via TOOL_METADATA. 6 annotation tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor tool_middleware to Metadata-Driven Filtering + Tests</name>
  <files>api/app/agent/tool_middleware.py, api/tests/test_tool_middleware.py</files>
  <read_first>
    - api/app/agent/tool_middleware.py
    - api/app/agent/tools/__init__.py (after Task 1 changes)
    - .planning/phases/13-skilldescriptor-enhancement-pipeline-fix/13-RESEARCH.md
  </read_first>
  <action>
**1. Refactor `api/app/agent/tool_middleware.py`** (per D-09, D-10):

Remove the 4 hardcoded name sets: _ALWAYS_TOOLS, _CANVAS_TOOLS, _STORYBOARD_TOOLS, _SCRIPT_TOOLS.

Replace the existing get_tools_for_context() implementation with metadata-driven logic:

```python
def get_tools_for_context(
    all_tools: list,
    *,
    has_canvas: bool = False,
    has_episode: bool = False,
) -> list:
    """Return at most 14 tools based on session context, using tool metadata (per D-09).

    Filtering rules (metadata-driven replacement of hardcoded name sets):
    - context_group == "always": always included (replaces _ALWAYS_TOOLS)
    - context_group == "canvas": included when has_canvas (replaces _CANVAS_TOOLS)
    - context_group == "episode": included when has_episode (replaces _STORYBOARD_TOOLS)
    - context_group == "script": included when NOT has_episode (replaces _SCRIPT_TOOLS)
    """
    selected = []
    for t in all_tools:
        meta = getattr(t, "metadata", None) or {}
        group = meta.get("context_group", "always")

        if group == "always":
            selected.append(t)
        elif group == "canvas" and has_canvas:
            selected.append(t)
        elif group == "episode" and has_episode:
            selected.append(t)
        elif group == "script" and not has_episode:
            selected.append(t)

    if len(selected) > 14:
        logger.warning(
            "Tool count %d exceeds target 14; consider refining metadata context_group values",
            len(selected),
        )

    return selected
```

Keep the tool_timeout decorator, _AI_TOOL_NAMES, TOOL_TIMEOUT_DEFAULT, TOOL_TIMEOUT_AI constants unchanged.

**2. Create `api/tests/test_tool_middleware.py`** with scenario tests:

Create a MockTool helper class with name and metadata attributes.
Build 17 mock tools using the TOOL_METADATA dict from tools/__init__.py.

Test scenarios:
- test_default_context_no_canvas_no_episode() — has_canvas=False, has_episode=False. Expected 10 tools: 7 "always" + 3 "script". Verify tool names include get_project_info, get_episodes, get_script, get_characters, get_scenes, read_skill, read_resource, save_characters, save_scenes, save_screenplay. Verify NO episode or canvas tools.
- test_episode_context() — has_canvas=False, has_episode=True. Expected 13 tools: 7 "always" + 6 "episode". Verify includes generate_image, generate_video, save_shot_plan, save_shot_details, update_shot, get_style_templates. Verify NO "script" tools (save_characters, save_scenes, save_screenplay NOT in result).
- test_canvas_context() — has_canvas=True, has_episode=False. Expected 11 tools: 7 "always" + 3 "script" + 1 "canvas". Verify get_canvas_state is included.
- test_both_context() — has_canvas=True, has_episode=True. Expected 14 tools: 7 "always" + 6 "episode" + 1 "canvas". No "script" tools.
- test_max_tool_count_respected() — All 4 scenarios produce at most 14 tools.
- test_tool_without_metadata_defaults_to_always() — A tool with metadata={} (no context_group) defaults to "always" and is included.
- test_tools_preserve_order() — Output tool order matches input order for selected tools.
  </action>
  <verify>
    <automated>cd api && uv run pytest tests/test_tool_middleware.py tests/test_skill_annotations.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tool_middleware.py does NOT contain `_ALWAYS_TOOLS = {` (hardcoded set removed)
    - tool_middleware.py does NOT contain `_CANVAS_TOOLS = {`
    - tool_middleware.py does NOT contain `_STORYBOARD_TOOLS = {`
    - tool_middleware.py does NOT contain `_SCRIPT_TOOLS = {`
    - tool_middleware.py contains `meta.get("context_group"` or equivalent metadata read
    - tool_middleware.py still contains `def tool_timeout` (unchanged)
    - test_tool_middleware.py contains at least 6 test functions
    - test_tool_middleware.py test_default_context asserts 10 tools
    - test_tool_middleware.py test_episode_context asserts 13 tools
    - test_tool_middleware.py test_both_context asserts 14 tools
    - `uv run pytest tests/test_tool_middleware.py -x` exits 0
  </acceptance_criteria>
  <done>tool_middleware.py uses metadata-driven filtering via context_group; hardcoded name sets removed; 7 test scenarios cover all context combinations; tool counts match pre-refactor behavior (10/11/13/14).</done>
</task>

</tasks>

<verification>
- `cd api && uv run pytest tests/test_skill_annotations.py tests/test_tool_middleware.py tests/test_descriptor_fields.py tests/test_skill_registration.py -x -v` — full Phase 13 test suite green
- `cd api && uv run pytest` — full project test suite green (no regressions)
- `grep -c 'skill_kind:' api/app/agent/skills/*/SKILL.md` — 10 matches (all SKILL.md annotated)
- `cd api && python -c "from app.agent.tools import get_all_tools; tools = get_all_tools(); print(len(tools), all(getattr(t, 'metadata', None) for t in tools))"` — prints `17 True`
- `cd api && python -c "from app.agent.tools import get_tools_for_context; print(len(get_tools_for_context(has_canvas=False, has_episode=False)))"` — prints `10`
- `cd api && python -c "from app.agent.tools import get_tools_for_context; print(len(get_tools_for_context(has_canvas=True, has_episode=True)))"` — prints `14`
</verification>

<success_criteria>
1. All 10 SKILL.md files have complete Phase 13 YAML frontmatter (DESC-07)
2. All 17 @tools have metadata dicts with skill_tier and context_group (DESC-07)
3. TOOL_METADATA has 17 entries covering all tools (DESC-07)
4. get_tools_for_context() reads metadata instead of hardcoded name sets (DESC-08)
5. Tool count per context: default=10, canvas=11, episode=13, both=14 (DESC-08)
6. All tests pass: test_skill_annotations.py + test_tool_middleware.py + existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/13-skilldescriptor-enhancement-pipeline-fix/13-02-SUMMARY.md`
</output>
