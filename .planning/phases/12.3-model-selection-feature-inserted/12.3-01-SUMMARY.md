---
phase: 12.3-model-selection-feature-inserted
plan: 01
subsystem: api
tags: [fastapi, sqlalchemy, model-selection, settings, resolve-fallback]

requires:
  - phase: 12.2-provider-model-preset-management-inserted
    provides: AIProviderConfig, AIModelConfig, ModelPricing models with FK associations
provides:
  - SystemSetting KV model for system-wide defaults
  - User.settings and Team.settings JSON columns for per-entity model defaults
  - GET /models/available endpoint returning filtered/grouped available models
  - resolve_provider_for_model() for model→provider derivation via ModelPricing JOIN
  - resolve_model_for_task() with 4-layer fallback (manual>project>user/team>system)
  - GET/PATCH /users/me/settings for personal model preferences
  - GET/PATCH /teams/{id}/settings for team model preferences
  - GET/PATCH /models/system-defaults for admin system defaults
affects: [12.3-02 frontend, 12.3-03 agent integration, canvas execution]

tech-stack:
  added: []
  patterns: [4-layer model resolution fallback, SystemSetting KV table, JSON settings merge with flag_modified]

key-files:
  created:
    - api/app/models/system_setting.py
    - api/app/schemas/models.py
    - api/app/api/v1/models.py
  modified:
    - api/app/models/user.py
    - api/app/models/team.py
    - api/app/core/database.py
    - api/app/services/ai/provider_manager.py
    - api/app/api/v1/router.py
    - api/app/api/v1/users.py
    - api/app/api/v1/teams.py

key-decisions:
  - "SystemSetting uses key as primary key (simple KV, no UUID) for direct db.get() access"
  - "system-defaults endpoints embedded in models.py router (not separate admin route) since model settings are model-domain"
  - "Task 3 (system-defaults) merged into Task 2 commit — same router file, no logical split needed"

patterns-established:
  - "JSON settings merge pattern: read current or {}, update keys, flag_modified(), flush()"
  - "4-layer model resolution: explicit > project.settings > user/team.settings > SystemSetting"

requirements-completed: [MSEL-01, MSEL-02, MSEL-03, MSEL-07, MSEL-09]

duration: 3min
completed: 2026-04-03
---

# Phase 12.3 Plan 01: Backend Model Selection Foundation Summary

**SystemSetting KV model, Available Models API with active-key filtering, 4-layer resolve_model_for_task fallback, and user/team/system settings endpoints**

## Performance

- **Duration:** 3 min
- **Started:** 2026-04-03T10:07:28Z
- **Completed:** 2026-04-03T10:10:46Z
- **Tasks:** 3
- **Files modified:** 10

## Accomplishments
- SystemSetting KV model with seeded defaults (gemini-2.5-flash / imagen-4.0-generate-001)
- User and Team models extended with settings JSON column + auto-migrate
- GET /models/available returns models grouped by llm/image, filtered to only those with active provider keys
- resolve_provider_for_model() derives provider from model name via ModelPricing JOIN
- resolve_model_for_task() implements 4-layer fallback: manual > project > personal/team > system
- Settings endpoints for user, team, and system-wide model defaults

## Task Commits

Each task was committed atomically:

1. **Task 1: Data model extensions** - `d6d2ecb` (feat)
2. **Task 2+3: Available Models API + resolve functions + settings endpoints** - `dac1508` (feat)

## Files Created/Modified
- `api/app/models/system_setting.py` - SystemSetting KV model (key, value, updated_at)
- `api/app/models/user.py` - Added settings JSON column
- `api/app/models/team.py` - Added settings JSON column
- `api/app/core/database.py` - Auto-migrate entries + SystemSetting import + seed defaults
- `api/app/schemas/models.py` - AvailableModel, AvailableModelsResponse, DefaultModelSettings
- `api/app/api/v1/models.py` - GET /available, GET/PATCH /system-defaults endpoints
- `api/app/api/v1/router.py` - Register models_router
- `api/app/services/ai/provider_manager.py` - resolve_provider_for_model + resolve_model_for_task
- `api/app/api/v1/users.py` - GET/PATCH /users/me/settings
- `api/app/api/v1/teams.py` - GET/PATCH /teams/{id}/settings

## Decisions Made
- SystemSetting uses key as primary key for direct db.get() access (no UUID overhead)
- system-defaults endpoints live in models.py router rather than a separate admin-only router
- Task 3 merged into Task 2's commit since they share the same router file

## Deviations from Plan

None - plan executed exactly as written (Task 3 was implemented alongside Task 2 since they target the same file).

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Backend APIs ready for frontend model selector (Plan 02)
- resolve_model_for_task ready for agent integration (Plan 03)
- All 8 success criteria satisfied

---
*Phase: 12.3-model-selection-feature-inserted*
*Completed: 2026-04-03*
