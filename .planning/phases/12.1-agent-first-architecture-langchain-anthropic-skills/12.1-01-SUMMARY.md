---
phase: 12.1-agent-first-architecture-langchain-anthropic-skills
plan: 01
subsystem: agent
tags: [langchain, langchain-core, langgraph, langsmith, pydantic, tool-calling, contextvars]

requires:
  - phase: 12-ai-call-convergence
    provides: ProviderManager async DB chain, KeyHealthManager, CredentialCache
provides:
  - 17 LangChain @tool pure-function tools across 4 modules (read, write, AI, skill)
  - contextvars-based tool context propagation with copy_context() safety
  - Tool gating middleware (≤14 tools per context) + timeout wrapper (60s/120s)
  - Tool aggregation with get_all_tools() and get_tools_for_context()
affects: [12.1-02-skills, 12.1-03-agent-engine, 12.1-04-api-integration, 12.1-05-cleanup]

tech-stack:
  added: [langchain, langchain-core, langgraph, langchain-google-genai, langchain-openai, langsmith, pyyaml]
  patterns: [LangChain @tool decorator with Pydantic args_schema, contextvars for session identity, tool gating by context]

key-files:
  created:
    - api/app/agent/tool_context.py
    - api/app/agent/tool_middleware.py
    - api/app/agent/tools/__init__.py
    - api/app/agent/tools/context_tools.py
    - api/app/agent/tools/mutation_tools.py
    - api/app/agent/tools/ai_tools.py
    - api/app/agent/tools/skill_tools.py
  modified:
    - api/pyproject.toml

key-decisions:
  - "Episode context replaces (not adds to) script tools in gating — keeps ≤14 tools"
  - "frozen=True on ToolContext dataclass for immutability safety"
  - "AI tools use inline asyncio.wait_for(120s) instead of decorator-based timeout"

patterns-established:
  - "LangChain @tool pattern: @tool(args_schema=XxxInput) async def xxx(...) -> str with JSON return"
  - "Self-contained DB access: each tool opens its own AsyncSessionLocal session"
  - "Auth guard pattern: get_tool_context().project_id check for cross-project security"

requirements-completed: [AGENT-01]

duration: 5min
completed: 2026-04-03
---

# Phase 12.1 Plan 01: LangChain Tool Toolkit Summary

**17 LangChain @tool pure functions (7 read, 6 write, 2 AI, 2 skill) with contextvars session propagation and context-aware gating (≤14 tools)**

## Performance

- **Duration:** 5 min
- **Started:** 2026-04-02T17:03:00Z
- **Completed:** 2026-04-02T17:08:17Z
- **Tasks:** 3
- **Files modified:** 8

## Accomplishments
- 17 LangChain @tool async functions across 4 modules with Pydantic BaseModel input schemas
- contextvars-based ToolContext with frozen dataclass and copy_context() for async boundary safety
- Tool gating middleware: default=10, +canvas=11, +episode=13, +both=14 (≤14 guaranteed)
- AI tools with 120s timeout, key health reporting, ContentBlockedError handling
- Skill tools with 100KB resource size cap and path traversal guard

## Task Commits

Each task was committed atomically:

1. **Task 1: LangChain deps + tool context + tool middleware** - `aaaefc8` (feat)
2. **Task 2: 13 data tools (7 read + 6 write)** - `623eede` (feat)
3. **Task 3: AI tools + skill tools + gating fix** - `3151ec4` (feat)

## Files Created/Modified
- `api/pyproject.toml` - Added 7 new dependencies (langchain stack + pyyaml)
- `api/app/agent/tool_context.py` - Session context propagation via contextvars
- `api/app/agent/tool_middleware.py` - Tool gating (≤14) + timeout wrapper (60s/120s)
- `api/app/agent/tools/__init__.py` - Tool aggregation with get_all_tools() and get_tools_for_context()
- `api/app/agent/tools/context_tools.py` - 7 read @tool functions (characters, scenes, script, canvas, project, episodes, style_templates)
- `api/app/agent/tools/mutation_tools.py` - 6 write @tool functions (save_characters, save_scenes, save_screenplay, save_shot_plan, save_shot_details, update_shot)
- `api/app/agent/tools/ai_tools.py` - 2 AI @tool wrappers (generate_image, generate_video) with 120s timeout
- `api/app/agent/tools/skill_tools.py` - 2 skill @tool functions (read_skill, read_resource) with 100KB cap

## Decisions Made
- Episode context replaces script tools in gating (mutually exclusive) to keep tool count ≤14
- ToolContext dataclass uses `frozen=True` for immutability safety across async boundaries
- AI tools embed `asyncio.wait_for(timeout=120)` directly rather than using the decorator, for explicit error handling with key health reporting

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed tool gating exceeding 14 when both canvas and episode active**
- **Found during:** Task 3 (verification step)
- **Issue:** Original gating logic added storyboard tools on top of script tools, resulting in 17 tools when both flags active
- **Fix:** Changed gating logic so episode context replaces script tools instead of adding to them
- **Files modified:** api/app/agent/tool_middleware.py
- **Verification:** `get_tools_for_context(has_canvas=True, has_episode=True)` returns 14
- **Committed in:** 3151ec4 (Task 3 commit)

---

**Total deviations:** 1 auto-fixed (1 bug)
**Impact on plan:** Bug fix ensures the plan's ≤14 tool invariant holds. No scope creep.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- 17 tools ready for Plan 02 (SkillLoader) and Plan 03 (Agent Engine) to consume
- Tool aggregation via `get_all_tools()` and `get_tools_for_context()` ready for LangGraph agent binding
- ToolContext ready for agent_service to call `set_tool_context()` before each agent invocation

## Self-Check: PASSED

---
*Phase: 12.1-agent-first-architecture-langchain-anthropic-skills*
*Completed: 2026-04-03*
