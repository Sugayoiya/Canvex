---
phase: 12.1-agent-first-architecture-langchain-anthropic-skills
plan: 04
subsystem: frontend
tags: [api-client, feature-flags, migration-telemetry, deprecation, canvas-adapter]

requires:
  - phase: 12.1-03
    provides: "LangChain AgentService + LangGraph SSE streaming + /agent/skills endpoint"
provides:
  - "Deprecated skillsApi.invoke and skillsApi.poll with @deprecated JSDoc"
  - "agentApi.listSkills() method pointing to /agent/skills"
  - "FEATURE_FLAGS.AGENT_CHAT_FOR_CANVAS for progressive canvas migration"
  - "Migration telemetry (trackEndpointUsage / getEndpointUsage)"
  - "Canvas node execution feature-flag adapter layer"
affects: [12.1-05-cleanup]

tech-stack:
  added: []
  patterns: [feature-flag-adapter, migration-telemetry, progressive-deprecation]

key-files:
  created:
    - web/src/lib/feature-flags.ts
  modified:
    - web/src/lib/api.ts
    - web/src/components/canvas/hooks/use-node-execution.ts

key-decisions:
  - "AGENT_CHAT_FOR_CANVAS defaults to false — legacy invoke remains active until agent proven stable"
  - "Migration telemetry via console.info in development mode — zero runtime cost in production"
  - "Canvas adapter falls through to legacy invoke even when flag=true (agent path is TODO for Phase 12.1B)"

requirements-completed: [AGENT-04]

duration: 1min
completed: 2026-04-03
---

# Phase 12.1 Plan 04: Frontend Agent Chat Wiring + Feature Flags Summary

**Deprecated legacy skillsApi.invoke/poll, added agentApi.listSkills, created feature-flag adapter for progressive canvas migration with endpoint usage telemetry**

## Performance

- **Duration:** 1 min
- **Started:** 2026-04-02T17:16:40Z
- **Completed:** 2026-04-02T17:17:58Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- `skillsApi.invoke` and `skillsApi.poll` marked with `@deprecated` JSDoc pointing to agent chat SSE
- `agentApi.listSkills()` added, calling `GET /agent/skills` for skill metadata
- `web/src/lib/feature-flags.ts` created with `AGENT_CHAT_FOR_CANVAS` flag (default: false)
- Migration telemetry tracks skills/invoke vs agent/chat endpoint usage counts
- Canvas node execution hook has feature-flag adapter — single routing decision point

## Task Commits

Each task was committed atomically:

1. **Task 1: Update API client + create feature flags module + add migration telemetry** — `d268dc0` (feat)
2. **Task 2: Add feature-flag adapter to canvas node execution** — `0d15468` (feat)

## Files Created/Modified

- `web/src/lib/feature-flags.ts` — NEW: Feature flag definitions (AGENT_CHAT_FOR_CANVAS), trackEndpointUsage(), getEndpointUsage()
- `web/src/lib/api.ts` — @deprecated on skillsApi.invoke/poll, added agentApi.listSkills()
- `web/src/components/canvas/hooks/use-node-execution.ts` — Feature-flag adapter + telemetry imports + JSDoc migration docs

## Decisions Made

- AGENT_CHAT_FOR_CANVAS defaults to `false` — legacy invoke remains the active path until agent stability is proven in chat-only mode
- Migration telemetry uses `console.info` in development mode only — zero overhead in production builds
- Canvas adapter intentionally falls through to legacy path even when flag=true (agent path is a TODO for Phase 12.1B) — no breakage risk

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

- `web/src/components/canvas/hooks/use-node-execution.ts` line ~183: `TODO(Phase 12.1B)` — agent chat path for canvas nodes not yet implemented (intentional; deferred to Phase 12.1B/13+ once agent proven stable)

## Issues Encountered

None.

## Next Phase Readiness

- Frontend fully adapted for agent-first architecture (Phase 12.1A bridge scope achieved)
- Chat-based AI operations route through agent chat SSE
- Canvas migration has clear feature-flag gate — flip `AGENT_CHAT_FOR_CANVAS` to enable
- Plan 05 (cleanup) can now remove PydanticAI remnants safely

## Self-Check: PASSED

---
*Phase: 12.1-agent-first-architecture-langchain-anthropic-skills*
*Completed: 2026-04-03*
