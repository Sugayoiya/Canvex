---
phase: 12.1-agent-first-architecture-langchain-anthropic-skills
plan: 03
subsystem: agent
tags: [langchain, langgraph, langchain-agents, create-agent, sse-streaming, multi-provider, langsmith]

requires:
  - phase: 12.1-01
    provides: "17 LangChain @tool functions + get_tools_for_context() + tool_context"
  - phase: 12.1-02
    provides: "SkillLoader with 3-level progressive disclosure + 10 SKILL.md directories"
provides:
  - "resolve_langchain_llm() on ProviderManager for Gemini/OpenAI/DeepSeek"
  - "LangChain-based AgentService with create_agent (replacing PydanticAI)"
  - "LangGraph SSE streaming with astream_events v2 + ainvoke fallback"
  - "System prompt with SkillLoader L1 metadata integration"
  - "LangChain message serialization (messages_to_dict/messages_from_dict)"
  - "/agent/skills listing endpoint"
affects: [12.1-04-api-integration, 12.1-05-cleanup]

tech-stack:
  added: [langchain-google-genai, langchain-openai]
  patterns: [langchain-create-agent, astream-events-v2-fallback, thread-safe-skill-loader-singleton]

key-files:
  modified:
    - api/app/services/ai/provider_manager.py
    - api/app/agent/agent_service.py
    - api/app/agent/context_builder.py
    - api/app/models/agent_session.py
    - api/app/api/v1/agent.py
    - api/app/agent/__init__.py

key-decisions:
  - "create_agent from langchain.agents (NOT deprecated create_react_agent from langgraph.prebuilt)"
  - "DeepSeek via ChatOpenAI(base_url=https://api.deepseek.com/v1) — verified working, NOT deprecated"
  - "Streaming fallback: astream_events(v2) primary → ainvoke terminal (skip step-level secondary)"
  - "Thread-safe SkillLoader singleton with double-checked locking + mtime hot-reload"
  - "langchain_messages_json column added alongside pydantic_ai_messages_json for backward compat"

patterns-established:
  - "resolve_langchain_llm pattern: ProviderManager resolves LangChain BaseChatModel via DB credential chain"
  - "Agent creation pattern: create_agent(llm, tools, system_prompt=system_prompt) returns CompiledStateGraph"
  - "SSE streaming pattern: astream_events(v2) → on_chat_model_stream/on_tool_start/on_tool_end → sse_* helpers"

requirements-completed: [AGENT-03]

duration: 5min
completed: 2026-04-03
---

# Phase 12.1 Plan 03: LangChain Agent Engine + SSE Streaming Summary

**LangChain create_agent replacing PydanticAI with multi-provider LLM switching (Gemini/OpenAI/DeepSeek), LangGraph SSE streaming with fallback matrix, and SkillLoader L1 system prompt integration**

## Performance

- **Duration:** 5 min
- **Started:** 2026-04-02T17:11:35Z
- **Completed:** 2026-04-02T17:16:35Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- ProviderManager extended with `resolve_langchain_llm()` supporting Gemini (ChatGoogleGenerativeAI), OpenAI (ChatOpenAI), and DeepSeek (ChatOpenAI with base_url)
- AgentService completely rewritten: PydanticAI Agent replaced with `create_agent` from `langchain.agents`, thread-safe SkillLoader singleton with double-checked locking
- Agent chat endpoint rewritten: PydanticAI `agent.iter()` replaced with LangGraph `astream_events(v2)` + `ainvoke` terminal fallback
- System prompt now includes SkillLoader L1 metadata (10 available skills listed in prompt)
- New `/agent/skills` endpoint for skill listing

## Task Commits

Each task was committed atomically:

1. **Task 1: resolve_langchain_llm + AgentService rewrite + context_builder + AgentMessage model** - `1c941f7` (feat)
2. **Task 2: Agent chat endpoint LangGraph SSE streaming + skills endpoint** - `5906be4` (feat)

## Files Created/Modified

- `api/app/services/ai/provider_manager.py` — Added `resolve_langchain_llm()` method to ProviderManager
- `api/app/agent/agent_service.py` — Complete rewrite: PydanticAI → LangChain create_agent, LangChain message serialization, thread-safe SkillLoader singleton
- `api/app/agent/context_builder.py` — Refactored to accept SkillLoader parameter, build system prompt with L1 skill metadata
- `api/app/models/agent_session.py` — Added `langchain_messages_json` column to AgentMessage
- `api/app/api/v1/agent.py` — Complete rewrite: LangGraph SSE streaming with fallback matrix, tool_context integration, /skills endpoint
- `api/app/agent/__init__.py` — Removed AgentDeps/SkillToolset re-exports (no longer exist)

## Decisions Made

- Used `create_agent` from `langchain.agents` (verified current API, NOT deprecated `create_react_agent` from `langgraph.prebuilt`)
- DeepSeek uses `ChatOpenAI(base_url='https://api.deepseek.com/v1')` — verified working and NOT deprecated (Codex review concern was incorrect)
- Streaming fallback simplified to 2-tier: primary `astream_events(v2)` → terminal `ainvoke` (step-level `astream` skipped as unnecessary intermediate)
- `langchain_messages_json` added as new column alongside `pydantic_ai_messages_json` for backward compatibility with existing sessions

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Fixed agent __init__.py re-exporting removed symbols**
- **Found during:** Task 1 verification
- **Issue:** `api/app/agent/__init__.py` imported `AgentDeps` and `SkillToolset` which no longer exist after the rewrite
- **Fix:** Updated `__init__.py` to only re-export `AgentService` and `SSEEventType`
- **Files modified:** api/app/agent/__init__.py
- **Verification:** Import succeeds without error
- **Committed in:** 1c941f7 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary fix for import chain. No scope creep.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required. LangSmith tracing auto-activates when `LANGCHAIN_TRACING_V2=true` is set in env.

## Next Phase Readiness

- LangChain agent engine fully operational, ready for Plan 04 (API integration / wiring)
- SSE streaming endpoint compatible with existing frontend (same sse_protocol event format)
- SkillLoader L1 metadata in system prompt — agent knows about all 10 skills
- Message history serialization switched to LangChain format (backward-compatible: old sessions return empty history)

## Self-Check: PASSED

---
*Phase: 12.1-agent-first-architecture-langchain-anthropic-skills*
*Completed: 2026-04-03*
