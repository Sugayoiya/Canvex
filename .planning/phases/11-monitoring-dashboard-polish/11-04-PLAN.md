---
phase: 11-monitoring-dashboard-polish
plan: 04
type: execute
wave: 3
depends_on: [11-02, 11-03]
files_modified:
  - web/src/app/admin/page.tsx
  - web/src/app/admin/monitoring/page.tsx
  - web/src/app/admin/users/page.tsx
  - web/src/app/admin/teams/page.tsx
  - web/src/app/admin/quotas/page.tsx
  - web/src/app/admin/pricing/page.tsx
  - web/src/app/admin/providers/page.tsx
autonomous: false
requirements: [REQ-24, REQ-25]

must_haves:
  truths:
    - "All 7 admin pages are wrapped with AdminErrorBoundary"
    - "All admin pages show loading skeletons during initial data fetch"
    - "All admin pages show error states when API calls fail"
    - "All admin pages with tables show empty states when data is empty"
    - "Admin chunks are not loaded for non-admin users visiting /projects"
  artifacts:
    - path: "web/src/app/admin/page.tsx"
      provides: "Dashboard with error boundary"
      contains: "AdminErrorBoundary"
    - path: "web/src/app/admin/monitoring/page.tsx"
      provides: "Monitoring with error boundary"
      contains: "AdminErrorBoundary"
    - path: "web/src/app/admin/users/page.tsx"
      provides: "Users with error boundary"
      contains: "AdminErrorBoundary"
  key_links:
    - from: "web/src/components/admin/admin-error-boundary.tsx"
      to: "all admin pages"
      via: "import and wrap content area"
      pattern: "AdminErrorBoundary"
---

<objective>
Wrap all admin pages with AdminErrorBoundary and audit/fix loading, error, and empty states across all 7 admin pages for production quality.

Purpose: Ensure all admin pages meet production quality bar (D-06 full polish audit). Error boundaries catch uncaught render errors. Loading skeletons, error states, and empty states are consistent across all admin pages.
Output: All 7 admin pages wrapped with AdminErrorBoundary; verified loading/error/empty state consistency.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-monitoring-dashboard-polish/11-CONTEXT.md
@.planning/phases/11-monitoring-dashboard-polish/11-UI-SPEC.md
@.planning/phases/11-monitoring-dashboard-polish/11-01-SUMMARY.md
@.planning/phases/11-monitoring-dashboard-polish/11-02-SUMMARY.md
@.planning/phases/11-monitoring-dashboard-polish/11-03-SUMMARY.md

<interfaces>
From web/src/components/admin/admin-error-boundary.tsx (created in Plan 01):
```typescript
export class AdminErrorBoundary extends Component<{ children: ReactNode }, { hasError: boolean }> {
  static getDerivedStateFromError(): { hasError: true };
  render(): ReactNode;  // Shows error card with "Something went wrong" + "Try Again" button
}
```

UI-SPEC Polish Audit Checklist:
| Page | Loading | Error | Empty | Toast |
|------|---------|-------|-------|-------|
| Dashboard | ✓ exists | ✓ exists | N/A | Refresh toast |
| Monitoring | Add per-tab | Add per-tab | Add per-tab | N/A |
| Users | Verify | Verify | Verify | ✓ exists |
| Teams | Verify | Verify | Verify | N/A |
| Quotas | Verify | Verify | Verify | ✓ exists |
| Pricing | Verify | Verify | Verify | ✓ exists |
| Providers | Verify | Verify | Verify | ✓ exists |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap all admin pages with AdminErrorBoundary + verify loading/error/empty consistency</name>
  <files>web/src/app/admin/page.tsx, web/src/app/admin/monitoring/page.tsx, web/src/app/admin/users/page.tsx, web/src/app/admin/teams/page.tsx, web/src/app/admin/quotas/page.tsx, web/src/app/admin/pricing/page.tsx, web/src/app/admin/providers/page.tsx</files>
  <read_first>
    - web/src/components/admin/admin-error-boundary.tsx (verify export and API)
    - web/src/app/admin/page.tsx (check existing loading/error states — already has dashPulse skeletons + error banner)
    - web/src/app/admin/monitoring/page.tsx (check per-tab loading/error/empty — should be handled by child table components)
    - web/src/app/admin/users/page.tsx (check loading/error/empty patterns)
    - web/src/app/admin/teams/page.tsx (check loading/error/empty patterns)
    - web/src/app/admin/quotas/page.tsx (check loading/error/empty patterns)
    - web/src/app/admin/pricing/page.tsx (check loading/error/empty patterns)
    - web/src/app/admin/providers/page.tsx (check loading/error/empty patterns)
    - .planning/phases/11-monitoring-dashboard-polish/11-UI-SPEC.md (Polish specifications section, error boundary placement, empty state copy)
  </read_first>
  <action>
**For each of the 7 admin pages, perform these steps:**

**Step 1: Add AdminErrorBoundary wrapper**

Import `{ AdminErrorBoundary }` from `@/components/admin/admin-error-boundary` and wrap the page content below the header. The page header (h1 + subtitle) should remain OUTSIDE the error boundary so it's always visible. Wrap the content area:

```typescript
import { AdminErrorBoundary } from "@/components/admin/admin-error-boundary";

// In the render:
<div style={{ display: "flex", flexDirection: "column", gap: 32 }}>
  {/* Page header stays outside */}
  <div>
    <h1>...</h1>
    <p>...</p>
  </div>
  <AdminErrorBoundary>
    {/* Content area wrapped */}
    {/* ... existing page content (tables, cards, etc.) ... */}
  </AdminErrorBoundary>
</div>
```

**Step 2: Audit and fix loading/error/empty states**

For each page, verify:

1. **Loading state:** Initial data fetch shows skeleton/spinner. AdminDataTable already has built-in `isLoading` skeletons. For pages using AdminDataTable, verify `isLoading` is passed. For dashboard, KPI skeletons already exist via `dashPulse`.

2. **Error state:** API failures show error UI with retry. AdminDataTable has built-in `isError` + `onRetry` props. For pages using AdminDataTable, verify `isError` and `onRetry` are passed. Dashboard already has inline error banner.

3. **Empty state:** Zero-data shows contextual message. AdminDataTable has built-in empty state via `emptyHeading`/`emptyBody`/`emptyIcon` props. Verify each page passes appropriate empty state copy.

**Specific fixes expected (verify each, fix if missing):**

| Page | Expected State | Fix if Missing |
|------|---------------|----------------|
| Dashboard (page.tsx) | Loading: ✓ dashPulse. Error: ✓ error banner. Empty: N/A (always has counts) | Add AdminErrorBoundary wrapper only |
| Monitoring (monitoring/page.tsx) | Loading/Error/Empty: handled by child table components | Add AdminErrorBoundary wrapper only |
| Users (users/page.tsx) | Loading: ✓ AdminDataTable. Error: ✓ AdminDataTable. Empty: verify emptyHeading/Body | Add AdminErrorBoundary wrapper; verify/add empty state props |
| Teams (teams/page.tsx) | Loading: ✓ AdminDataTable. Error: ✓ AdminDataTable. Empty: verify emptyHeading/Body | Add AdminErrorBoundary wrapper; verify/add empty state props |
| Quotas (quotas/page.tsx) | Loading: verify. Error: verify. Empty: verify | Add AdminErrorBoundary wrapper; fix any missing states |
| Pricing (pricing/page.tsx) | Loading: ✓ AdminDataTable. Error: ✓ AdminDataTable. Empty: verify | Add AdminErrorBoundary wrapper; verify/add empty state props |
| Providers (providers/page.tsx) | Loading: verify. Error: verify. Empty: verify | Add AdminErrorBoundary wrapper; fix any missing states |

**Key rule:** Do NOT restructure page logic. Only add the AdminErrorBoundary import+wrap, and fix any missing loading/error/empty state props on existing components. The AdminDataTable already handles these internally — just verify the props are being passed correctly.
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - web/src/app/admin/page.tsx contains `import { AdminErrorBoundary }` (or equivalent)
    - web/src/app/admin/page.tsx contains `<AdminErrorBoundary>`
    - web/src/app/admin/monitoring/page.tsx contains `<AdminErrorBoundary>`
    - web/src/app/admin/users/page.tsx contains `<AdminErrorBoundary>`
    - web/src/app/admin/teams/page.tsx contains `<AdminErrorBoundary>`
    - web/src/app/admin/quotas/page.tsx contains `<AdminErrorBoundary>`
    - web/src/app/admin/pricing/page.tsx contains `<AdminErrorBoundary>`
    - web/src/app/admin/providers/page.tsx contains `<AdminErrorBoundary>`
    - All 7 pages have AdminErrorBoundary wrapping content below header
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>All 7 admin pages wrapped with AdminErrorBoundary; loading/error/empty states verified and consistent across all pages</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 11 delivery</name>
  <files>web/src/app/admin/page.tsx, web/src/app/admin/monitoring/page.tsx</files>
  <what-built>Complete Phase 11: admin dashboard with actionable KPI cards, 4-tab monitoring page, and production polish across all admin pages</what-built>
  <action>Human verifies the complete Phase 11 delivery in browser.</action>
  <how-to-verify>
    1. Visit http://localhost:3000/admin — Dashboard shows KPI cards; hover shows blue glow + lift; click navigates to sub-pages
    2. Check alert badges appear on KPI cards if there are quota warnings or failed tasks
    3. Visit http://localhost:3000/admin/monitoring — 4 tabs render: Tasks, AI Calls, Skills, Usage & Cost
    4. Switch between tabs — each shows its table or charts
    5. Test search filter with 300ms debounce on Tasks tab
    6. Test status filter dropdown on Tasks tab
    7. Visit Usage & Cost tab — Recharts line chart + pie chart render
    8. For each admin page (users/teams/quotas/pricing/providers): verify no uncaught errors, loading skeletons appear on slow network, error states show on API failure
    9. Verify dashboard fits in one viewport at 1920x1080 without scrolling
    10. Open browser DevTools Network tab → navigate to /projects → confirm no admin-* chunks loaded
  </how-to-verify>
  <verify>Manual browser verification of all admin pages</verify>
  <done>All admin pages visually verified: KPI cards clickable, monitoring tabs functional, error boundaries active, loading/empty states consistent</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- All 7 admin pages render without errors
- AdminErrorBoundary catches intentional errors (test by temporarily breaking a component)
- All pages show loading skeletons during initial fetch
- All pages with tables show empty states when no data
- Dashboard fits one viewport without scrolling
- Admin chunks are code-split from non-admin routes
- `cd web && npx tsc --noEmit` passes
</verification>

<success_criteria>
All admin pages are production-quality with consistent error handling, loading states, and empty states. AdminErrorBoundary catches render errors gracefully. Bundle isolation confirmed.
</success_criteria>

<output>
After completion, create `.planning/phases/11-monitoring-dashboard-polish/11-04-SUMMARY.md`
</output>
