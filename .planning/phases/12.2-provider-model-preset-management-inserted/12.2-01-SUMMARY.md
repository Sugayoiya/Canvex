---
phase: 12.2-provider-model-preset-management-inserted
plan: 01
subsystem: database, api
tags: [sqlalchemy, orm, preset, seed, provider-meta, pricing]

requires:
  - phase: 12-ai-call-convergence
    provides: AIProviderConfig/AIProviderKey/AIModelConfig ORM models, ProviderManager DB credential chain

provides:
  - Extended AIProviderConfig with preset/icon/sdk_type/base_url/seed_version fields
  - Extended AIModelConfig with preset/token-limit/seed_version fields
  - ModelPricing FK fields (provider_config_id, model_config_id) linking to config tables
  - AIModelProviderMapping deleted (replaced by ModelPricing as association)
  - PROVIDER_META + default_pricing in gemini/openai/deepseek providers
  - preset_seed.py centralized seed logic with seed_version gating
  - init_db() wired to seed 3 providers + 8 models + 8 pricing rows on startup

affects: [12.2-02-PLAN, 12.2-03-PLAN]

tech-stack:
  added: []
  patterns: [seed_version gating for preset data, PROVIDER_META declarations in provider files, ModelPricing as provider-model FK association]

key-files:
  created:
    - api/app/services/ai/preset_seed.py
  modified:
    - api/app/models/ai_provider_config.py
    - api/app/models/model_pricing.py
    - api/app/services/ai/model_providers/gemini.py
    - api/app/services/ai/model_providers/openai_provider.py
    - api/app/services/ai/model_providers/deepseek.py
    - api/app/core/database.py
    - api/app/api/v1/ai_providers.py
    - api/app/services/ai/provider_manager.py

key-decisions:
  - "ModelPricing serves as the provider-model association table (replaces AIModelProviderMapping)"
  - "seed_version gating prevents overwriting user-modified records during re-seed"
  - "Env-seeded providers also marked is_preset=True with PROVIDER_META fields applied"

patterns-established:
  - "PROVIDER_META dict in each provider file: provider_name, display_name, description, icon, sdk_type, default_base_url"
  - "default_pricing dict in each _KNOWN_MODELS entry: pricing_model + price fields"
  - "seed_version integer gating: code CURRENT_SEED_VERSION > DB seed_version to permit update"

requirements-completed: [PROV-01, PROV-02, PROV-03]

duration: 4min
completed: 2026-04-03
---

# Phase 12.2 Plan 01: Backend Preset Data Models & Seed Summary

**Extended ORM models with preset/seed/FK fields, added PROVIDER_META + default_pricing to 3 providers, created centralized preset_seed.py seeding 3 providers + 8 models + 8 pricing rows on startup**

## Performance

- **Duration:** 4 min
- **Started:** 2026-04-03T04:52:47Z
- **Completed:** 2026-04-03T04:56:24Z
- **Tasks:** 2
- **Files modified:** 9

## Accomplishments

- Extended AIProviderConfig with 7 new fields (is_preset, description, icon, sdk_type, default_base_url, base_url, seed_version) and AIModelConfig with 4 new fields (is_preset, input_token_limit, output_token_limit, seed_version)
- Deleted AIModelProviderMapping class and replaced with ModelPricing FK association (provider_config_id, model_config_id)
- Added PROVIDER_META + default_pricing to all 3 provider implementations (gemini, openai, deepseek) — 8 model entries total
- Created preset_seed.py with get_all_provider_metadata(), seed_preset_providers(), seed_preset_models_and_pricing() — wired into init_db()

## Task Commits

Each task was committed atomically:

1. **Task 1: Extend ORM models + Add PROVIDER_META** - `2ec22a8` (feat)
2. **Task 2: Create preset_seed.py + Wire into init_db()** - `0298cf6` (feat)

## Files Created/Modified

- `api/app/models/ai_provider_config.py` - Extended AIProviderConfig + AIModelConfig, deleted AIModelProviderMapping
- `api/app/models/model_pricing.py` - Added provider_config_id/model_config_id FK fields + relationships
- `api/app/services/ai/model_providers/gemini.py` - Added PROVIDER_META + default_pricing to _KNOWN_MODELS
- `api/app/services/ai/model_providers/openai_provider.py` - Added PROVIDER_META + default_pricing to _KNOWN_MODELS
- `api/app/services/ai/model_providers/deepseek.py` - Added PROVIDER_META + default_pricing to _KNOWN_MODELS
- `api/app/core/database.py` - Added 13 auto-migrate entries, wired preset seed into init_db(), removed AIModelProviderMapping import
- `api/app/api/v1/ai_providers.py` - Removed AIModelProviderMapping import, simplified list_models endpoint
- `api/app/services/ai/preset_seed.py` - New: centralized preset seeding logic
- `api/app/services/ai/provider_manager.py` - Updated seed_providers_from_env to set is_preset=True + PROVIDER_META fields

## Decisions Made

- ModelPricing replaces AIModelProviderMapping as the provider-model association table, keeping both text fields (provider, model) and FK fields (provider_config_id, model_config_id) for backward compatibility per D-13
- seed_version gating prevents overwriting user-modified records — only updates when code CURRENT_SEED_VERSION > DB record seed_version
- list_models endpoint temporarily returns empty providers list (Plan 02 will wire ModelPricing join)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Data foundation complete: preset providers, models, and pricing associations seeded on startup
- Plan 02 can now build API endpoints and admin schemas on top of the extended models
- Plan 03 can build the frontend admin UI consuming the new endpoint data

---
*Phase: 12.2-provider-model-preset-management-inserted*
*Completed: 2026-04-03*
