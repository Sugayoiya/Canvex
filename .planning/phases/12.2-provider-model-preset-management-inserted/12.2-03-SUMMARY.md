---
phase: 12.2-provider-model-preset-management-inserted
plan: 03
subsystem: frontend
tags: [react, provider-ui, model-list, preset-management, dify-style]

requires:
  - phase: 12.2-provider-model-preset-management-inserted
    plan: 02
    provides: Model CRUD endpoints, extended ProviderConfigResponse with preset/sdk_type/base_url fields

provides:
  - ProviderIcon component with gemini/openai/deepseek SVGs + Bot fallback
  - ProviderModelList component with collapsible model rows, toggle, add-model form
  - Restructured AdminProvidersPage with 已配置/待配置 provider groups
  - Simplified ProviderFormModal (Key + optional Base URL only)
  - Extended Provider type with sdk_type, is_preset, description, icon, base_url fields
  - aiProvidersApi methods: listProviderModels, createProviderModel, updateProviderModel

affects: []

tech-stack:
  added: []
  patterns: [provider group partitioning by active_key_count + is_enabled, inline SVG provider icons, collapsible model list with independent expand state]

key-files:
  created:
    - web/src/components/admin/provider-icon.tsx
    - web/src/components/admin/provider-model-list.tsx
  modified:
    - web/src/lib/api.ts
    - web/src/app/admin/providers/page.tsx
    - web/src/components/admin/provider-form-modal.tsx
    - web/src/components/admin/provider-card.tsx

key-decisions:
  - "Provider type extended with optional fields to avoid breaking existing consumers"
  - "ProviderFormModal now self-contained with own addKey mutation instead of receiving onSubmit callback"
  - "Model list collapsible state independent of card expand — reduces visual noise"

patterns-established:
  - "Group partitioning: active_key_count > 0 && is_enabled → 已配置, rest → 待配置"
  - "ProviderIcon switch(name) pattern for brand SVGs with Bot fallback"

requirements-completed: [PROV-04, PROV-05, PROV-06]

duration: 5min
completed: 2026-04-03
---

# Phase 12.2 Plan 03: Frontend Provider & Model Preset UI Summary

**Restructured admin providers page with Dify-style 已配置/待配置 groups, ProviderIcon brand SVGs, simplified key-only config modal, and embedded collapsible model lists per provider card**

## Performance

- **Duration:** 5 min
- **Started:** 2026-04-03T05:02:54Z
- **Completed:** 2026-04-03T05:08:00Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- Created ProviderIcon component with inline SVGs for gemini (4-color gradient), openai (official logo), deepseek + Lucide Bot fallback
- Added 3 API client methods: listProviderModels, createProviderModel, updateProviderModel
- Restructured AdminProvidersPage: removed "Add Provider" button, createMutation, deleteMutation, delete modal; split providers into "已配置"/"待配置" groups
- Simplified ProviderFormModal to Key + optional Base URL (shown only for openai_compatible sdk_type), self-contained mutation
- Created ProviderModelList with collapsible model rows (name/type/context/pricing/toggle), inline add-model form, toggle mutation with toast
- Updated ProviderCard: ProviderIcon replaces Bot, Settings replaces Pencil, removed delete button/prop, added description line, embedded ProviderModelList
- Extended Provider type with sdk_type, is_preset, description, icon, default_base_url, base_url fields

## Task Commits

Each task was committed atomically:

1. **Task 1: ProviderIcon + API client + Restructure page + Simplify modal** - `9927341` (feat)
2. **Task 2: ProviderModelList + Embed in ProviderCard** - `2548a2c` (feat)

## Files Created/Modified

- `web/src/components/admin/provider-icon.tsx` - New: ProviderIcon with gemini/openai/deepseek inline SVGs + Bot fallback
- `web/src/components/admin/provider-model-list.tsx` - New: Collapsible model list with rows, toggle, add-model form
- `web/src/lib/api.ts` - Added listProviderModels, createProviderModel, updateProviderModel to aiProvidersApi
- `web/src/app/admin/providers/page.tsx` - Restructured with 已配置/待配置 groups, removed create/delete mutations, simplified form modal integration
- `web/src/components/admin/provider-form-modal.tsx` - Simplified to Key + Base URL, self-contained addKey mutation, validation
- `web/src/components/admin/provider-card.tsx` - ProviderIcon, Settings icon, removed delete, added description + model list, updated status display

## Decisions Made

- Provider type extended with optional fields (sdk_type?, is_preset?, etc.) to avoid breaking existing non-admin consumers
- ProviderFormModal now owns its own addKey mutation — cleaner than passing callbacks from page
- Model list has independent collapsible state (collapsed by default) to reduce visual noise in expanded cards

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Made onDelete optional before full removal**
- **Found during:** Task 1
- **Issue:** Page removed onDelete invocations but ProviderCard still required it in props, causing TS error
- **Fix:** Made onDelete optional in ProviderCardProps, added optional chaining; fully removed in Task 2
- **Files modified:** web/src/components/admin/provider-card.tsx
- **Commit:** 9927341

## Known Stubs

None — all components are wired to real API endpoints.

## Self-Check: PASSED

All 6 files exist. Both commits verified: 9927341, 2548a2c.
