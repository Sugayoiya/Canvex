---
phase: 12.2-provider-model-preset-management-inserted
verified: 2026-04-03T05:25:00Z
status: passed
score: 7/7 must-haves verified
must_haves:
  truths:
    - "系统启动时自动种子 Gemini/OpenAI/DeepSeek 预置 Provider（is_preset=True 不可删），每个含 icon/description/default_base_url"
    - "预置常用模型（≥7 个 LLM + Image 模型）写入 AIModelConfig（is_preset=True），并通过 ModelPricing 关联到对应 Provider"
    - "ModelPricing 同时作为 Provider-Model 关联表和定价表，AIModelProviderMapping 删除"
    - "Provider 配置弹窗简化为只需填 API Key + 可选 Base URL"
    - "Provider 卡片内可展开模型列表，显示名称/类型/context_size/定价/启用状态"
    - "用户可手动添加自定义模型并关联到指定 Provider"
    - "base_url 字段正确传递到 resolve_langchain_llm，支持 OpenAI 兼容类 Provider 自定义端点"
  artifacts:
    - path: "api/app/services/ai/preset_seed.py"
      provides: "Centralized preset seeding logic"
    - path: "api/app/models/ai_provider_config.py"
      provides: "Extended AIProviderConfig + AIModelConfig models (no AIModelProviderMapping)"
    - path: "api/app/models/model_pricing.py"
      provides: "ModelPricing with FK fields"
    - path: "api/app/services/ai/provider_manager.py"
      provides: "DB-backed resolve_langchain_llm"
    - path: "api/app/api/v1/ai_providers.py"
      provides: "Model CRUD + provider models endpoint + preset protection"
    - path: "api/app/schemas/ai_provider.py"
      provides: "Extended response schemas with preset/model fields"
    - path: "web/src/components/admin/provider-icon.tsx"
      provides: "ProviderIcon component (gemini/openai/deepseek SVGs)"
    - path: "web/src/components/admin/provider-model-list.tsx"
      provides: "Collapsible model list for provider card"
    - path: "web/src/app/admin/providers/page.tsx"
      provides: "Restructured admin providers page with groups"
    - path: "web/src/components/admin/provider-form-modal.tsx"
      provides: "Simplified provider config modal"
  key_links:
    - from: "api/app/services/ai/preset_seed.py"
      to: "api/app/models/ai_provider_config.py"
      via: "creates AIProviderConfig + AIModelConfig rows"
    - from: "api/app/services/ai/preset_seed.py"
      to: "api/app/models/model_pricing.py"
      via: "creates ModelPricing rows with FK fields"
    - from: "api/app/core/database.py"
      to: "api/app/services/ai/preset_seed.py"
      via: "calls seed functions in init_db()"
    - from: "api/app/services/ai/provider_manager.py"
      to: "api/app/models/ai_provider_config.py"
      via: "reads AIProviderConfig.sdk_type + base_url from DB"
    - from: "api/app/api/v1/ai_providers.py"
      to: "api/app/models/model_pricing.py"
      via: "joins ModelPricing for provider models list"
    - from: "web/src/components/admin/provider-model-list.tsx"
      to: "web/src/lib/api.ts"
      via: "aiProvidersApi.listProviderModels(providerId)"
    - from: "web/src/components/admin/provider-form-modal.tsx"
      to: "web/src/lib/api.ts"
      via: "aiProvidersApi.addKey + aiProvidersApi.update"
    - from: "web/src/components/admin/provider-card.tsx"
      to: "web/src/components/admin/provider-model-list.tsx"
      via: "renders ProviderModelList in expanded area"
---

# Phase 12.2: Provider & Model Preset Management Verification Report

**Phase Goal:** 将 Provider 管理从"用户手动创建"改为"系统预置 + 用户填写 Key/BaseURL"模式，预置常用 Model，用 ModelPricing 作为 Provider-Model 关联兼定价表，模型列表嵌入 Provider 卡片内展示（Dify 风格）
**Verified:** 2026-04-03T05:25:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | 系统启动时自动种子 Gemini/OpenAI/DeepSeek 预置 Provider（is_preset=True 不可删），每个含 icon/description/default_base_url | ✓ VERIFIED | `preset_seed.py` lines 38-77: creates 3 providers with `is_preset=True`, icon, description, sdk_type, default_base_url. `database.py` line 112-116: calls `seed_preset_providers()` in `init_db()`. `ai_providers.py` line 197-198: delete returns 403 for `is_preset=True`. |
| 2 | 预置常用模型（≥7 个 LLM + Image 模型）写入 AIModelConfig（is_preset=True），并通过 ModelPricing 关联到对应 Provider | ✓ VERIFIED | 8 models total with `default_pricing`: gemini (3: flash, pro, flash-image), openai (3: gpt-4o, gpt-4o-mini, gpt-4.1-mini), deepseek (2: chat, reasoner). `preset_seed.py` lines 80-190: creates AIModelConfig with `is_preset=True` + ModelPricing with FK fields. |
| 3 | ModelPricing 同时作为 Provider-Model 关联表和定价表，AIModelProviderMapping 删除 | ✓ VERIFIED | `model_pricing.py` lines 46-54: has `provider_config_id` and `model_config_id` ForeignKeys + relationships. `rg AIModelProviderMapping api/` returns 0 matches — fully deleted. `ai_providers.py` line 436-444: `list_models` uses `ModelPricing.model_config_id` join. |
| 4 | Provider 配置弹窗简化为只需填 API Key + 可选 Base URL | ✓ VERIFIED | `provider-form-modal.tsx`: shows API Key (password input) + conditional Base URL (line 29: `showBaseUrl = provider?.sdk_type === "openai_compatible"`). Title is "配置 {provider.display_name}" (line 178). Submit button "保存配置" (line 270). No provider_name/display_name/priority fields. |
| 5 | Provider 卡片内可展开模型列表，显示名称/类型/context_size/定价/启用状态 | ✓ VERIFIED | `provider-model-list.tsx`: ModelRow renders 5 columns — name (flex:1), type (64px), context (80px), pricing (100px), toggle (48px). Fetches via `aiProvidersApi.listProviderModels`. Shows "未定价" for no pricing. `provider-card.tsx` line 652: `<ProviderModelList providerId={provider.id} />` embedded in expanded area. |
| 6 | 用户可手动添加自定义模型并关联到指定 Provider | ✓ VERIFIED | `provider-model-list.tsx` lines 83-89 + 167-238: inline form with model_name input + model_type select + "添加模型" button. Calls `aiProvidersApi.createProviderModel`. Backend `ai_providers.py` lines 502-553: `POST /{provider_id}/models` creates AIModelConfig + ModelPricing. |
| 7 | base_url 字段正确传递到 resolve_langchain_llm，支持 OpenAI 兼容类 Provider 自定义端点 | ✓ VERIFIED | `provider_manager.py` lines 374-390: reads `config.sdk_type` and `config.base_url or config.default_base_url` from DB. Dispatches: `sdk_type == "native"` → ChatGoogleGenerativeAI, else → ChatOpenAI with `base_url`. `_get_provider_config` (lines 392-416) queries system-level AIProviderConfig. |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `api/app/services/ai/preset_seed.py` | Centralized preset seeding logic | ✓ VERIFIED | 191 lines. Exports `get_all_provider_metadata`, `seed_preset_providers`, `seed_preset_models_and_pricing`. Seed_version gating implemented. |
| `api/app/models/ai_provider_config.py` | Extended AIProviderConfig + AIModelConfig | ✓ VERIFIED | 68 lines. AIProviderConfig has 7 new fields (is_preset, description, icon, sdk_type, default_base_url, base_url, seed_version). AIModelConfig has 4 new fields (is_preset, input_token_limit, output_token_limit, seed_version). No AIModelProviderMapping class. |
| `api/app/models/model_pricing.py` | ModelPricing with FK fields | ✓ VERIFIED | 60 lines. Has `provider_config_id` + `model_config_id` ForeignKeys with indexes, plus `provider_config` + `model_config` relationships. |
| `api/app/services/ai/provider_manager.py` | DB-backed resolve_langchain_llm | ✓ VERIFIED | 546 lines. `resolve_langchain_llm` uses `config.sdk_type` dispatch (not hardcoded provider_name checks). `_get_provider_config` method queries DB. `seed_providers_from_env` sets `is_preset=True` + PROVIDER_META fields. |
| `api/app/api/v1/ai_providers.py` | Model CRUD + provider models endpoint + preset protection | ✓ VERIFIED | 599 lines. Has `GET /{provider_id}/models`, `POST /{provider_id}/models`, `PATCH /{provider_id}/models/{model_id}`. Delete returns 403 for preset. `_config_to_response` includes all preset/base_url fields. |
| `api/app/schemas/ai_provider.py` | Extended response schemas | ✓ VERIFIED | 148 lines. Has `ProviderConfigResponse` with description/icon/sdk_type/default_base_url/base_url/is_preset. Has `ProviderModelResponse`, `ModelPricingBrief`, `ModelCreateRequest`, `ModelUpdateRequest`. |
| `web/src/components/admin/provider-icon.tsx` | ProviderIcon with brand SVGs | ✓ VERIFIED | 58 lines. Handles gemini (4-color gradient), openai (official logo SVG), deepseek, defaults to Lucide Bot. |
| `web/src/components/admin/provider-model-list.tsx` | Collapsible model list | ✓ VERIFIED | 380 lines. Fetches via `listProviderModels`. ModelRow with 5 columns. Toggle mutation with toast. Inline add-model form. |
| `web/src/app/admin/providers/page.tsx` | Restructured admin page | ✓ VERIFIED | 484 lines. No "Add Provider" button, no createMutation, no deleteMutation. Groups: 已配置 / 待配置 (line 173-178). |
| `web/src/components/admin/provider-form-modal.tsx` | Simplified config modal | ✓ VERIFIED | 278 lines. API Key + conditional Base URL only. Self-contained addKey mutation. Title: "配置 {provider.display_name}". |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `preset_seed.py` | `ai_provider_config.py` | Creates AIProviderConfig rows | ✓ WIRED | `AIProviderConfig(` at line 62 |
| `preset_seed.py` | `model_pricing.py` | Creates ModelPricing rows | ✓ WIRED | `ModelPricing(` at line 176 |
| `database.py` | `preset_seed.py` | Calls seed in init_db() | ✓ WIRED | Lines 112-116: imports + calls seed_preset_providers + seed_preset_models_and_pricing |
| `provider_manager.py` | `ai_provider_config.py` | Reads sdk_type + base_url | ✓ WIRED | Line 376: `sdk_type = config.sdk_type if config else "native"` |
| `ai_providers.py` | `model_pricing.py` | Joins for provider models | ✓ WIRED | Line 461: `ModelPricing.model_config_id == AIModelConfig.id` |
| `provider-model-list.tsx` | `api.ts` | listProviderModels call | ✓ WIRED | Line 43: `aiProvidersApi.listProviderModels(providerId)` |
| `provider-form-modal.tsx` | `api.ts` | addKey call | ✓ WIRED | Line 77: `aiProvidersApi.addKey(provider.id, ...)` |
| `provider-card.tsx` | `provider-model-list.tsx` | Renders ProviderModelList | ✓ WIRED | Line 652: `<ProviderModelList providerId={provider.id} />` |
| `api.ts` | Backend endpoints | 3 new API methods | ✓ WIRED | Lines 349-354: listProviderModels, createProviderModel, updateProviderModel |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `provider-model-list.tsx` | `models` | `aiProvidersApi.listProviderModels → GET /ai-providers/{id}/models` | Yes: backend joins AIModelConfig + ModelPricing from DB | ✓ FLOWING |
| `providers/page.tsx` | `providers` | `aiProvidersApi.list → GET /ai-providers/` | Yes: backend queries AIProviderConfig with keys from DB | ✓ FLOWING |
| `provider-form-modal.tsx` | `provider` prop | Passed from page via `formModal.provider` | Yes: comes from providers query | ✓ FLOWING |

### Behavioral Spot-Checks

Step 7b: SKIPPED (server not running — requires full backend + database startup)

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| PROV-01 | Plan 01 | 系统启动时自动种子 3 预置 Provider | ✓ SATISFIED | preset_seed.py seeds 3 providers on startup via init_db() |
| PROV-02 | Plan 01 | 预置常用模型（≥7 个）通过 ModelPricing 关联 | ✓ SATISFIED | 8 models total (3+3+2), all with default_pricing + ModelPricing FK |
| PROV-03 | Plan 01 | ModelPricing 作关联表，AIModelProviderMapping 删除 | ✓ SATISFIED | ModelPricing has FK fields, grep for AIModelProviderMapping returns 0 |
| PROV-04 | Plans 02, 03 | Provider 配置弹窗简化为 Key + Base URL | ✓ SATISFIED | provider-form-modal.tsx: API Key + conditional Base URL only |
| PROV-05 | Plans 02, 03 | Provider 卡片内可展开模型列表 | ✓ SATISFIED | provider-model-list.tsx embedded in provider-card.tsx, shows 5 columns |
| PROV-06 | Plans 02, 03 | 用户可手动添加自定义模型 | ✓ SATISFIED | Inline form + POST endpoint creates model + pricing association |
| PROV-07 | Plan 02 | base_url 正确传递到 resolve_langchain_llm | ✓ SATISFIED | resolve_langchain_llm reads sdk_type + base_url from DB, dispatches correctly |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | No anti-patterns found | — | — |

Scanned all 10 key files for TODO, FIXME, PLACEHOLDER, placeholder, "coming soon", "not yet implemented", return null/empty stubs. Zero matches.

### Human Verification Required

### 1. Visual Provider Card Appearance

**Test:** Open Admin → Providers page, verify 3 preset providers displayed with correct icons (Gemini 4-color diamond, OpenAI logo, DeepSeek logo)
**Expected:** Each card shows correct SVG icon, display name, description. Cards grouped into "已配置" / "待配置" sections.
**Why human:** Visual SVG rendering quality and icon recognition cannot be verified programmatically.

### 2. End-to-End Key Configuration Flow

**Test:** Click configure (⚙️) on a "待配置" provider → enter API key → submit → verify provider moves to "已配置" group
**Expected:** Modal shows "配置 {name}", API Key input, submit "保存配置". After success, toast appears, provider card moves to 已配置 group.
**Why human:** Requires running app with real database state transitions.

### 3. Model List Expand/Collapse

**Test:** Expand a configured provider card → click "模型列表 (N)" header → verify models display with pricing
**Expected:** Models shown with name, type, context window, pricing (e.g., $0.00015/$0.0006), enable/disable toggle. Collapsible independent of card expand.
**Why human:** Interactive collapse behavior and layout rendering.

### 4. Custom Model Addition

**Test:** In expanded model list, type a model name → select type → click "添加模型" → verify model appears
**Expected:** Model created with toast "已添加模型: {name}", appears in model list, visible on page refresh.
**Why human:** Requires running app with database write.

### Gaps Summary

No gaps found. All 7 observable truths verified. All 10 artifacts exist, are substantive, wired, and have flowing data. All 8 key links verified. All 7 requirements (PROV-01 through PROV-07) satisfied. Zero anti-patterns detected.

---

_Verified: 2026-04-03T05:25:00Z_
_Verifier: Claude (gsd-verifier)_
