---
phase: 06-collaboration-prod
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/app/globals.css
  - web/src/app/layout.tsx
  - web/src/components/auth/auth-guard.tsx
  - web/src/components/providers.tsx
  - web/src/stores/auth-store.ts
  - web/src/lib/api.ts
autonomous: true
requirements: [REQ-11]
must_haves:
  truths:
    - "Obsidian Lens CSS custom properties defined under --ob-* namespace"
    - "AuthGuard wraps all routes and redirects unauthenticated users to /login"
    - "Auth store tracks teams, currentSpace, switchSpace action"
    - "API client has teamsApi, projectsApi, aiProvidersApi, usersApi exports"
    - "Space Grotesk and Manrope fonts loaded via next/font/google"
  artifacts:
    - path: "web/src/app/globals.css"
      provides: "Obsidian Lens design tokens + dot-grid background"
      contains: "--ob-primary"
    - path: "web/src/components/auth/auth-guard.tsx"
      provides: "Global auth protection component"
      contains: "export function AuthGuard"
    - path: "web/src/stores/auth-store.ts"
      provides: "Extended auth store with team/space state"
      contains: "currentSpace"
    - path: "web/src/lib/api.ts"
      provides: "All new API client namespaces"
      contains: "teamsApi"
  key_links:
    - from: "web/src/components/providers.tsx"
      to: "web/src/components/auth/auth-guard.tsx"
      via: "AuthGuard wraps children inside Providers"
      pattern: "AuthGuard"
    - from: "web/src/app/layout.tsx"
      to: "web/src/app/globals.css"
      via: "CSS import for design tokens"
      pattern: "globals\\.css"
---

<objective>
Establish frontend infrastructure for Phase 06: Obsidian Lens design system tokens, global AuthGuard, extended auth store with team/space switching, and comprehensive API client.

Purpose: All frontend pages in Plans 06-07 depend on these tokens, auth protection, and API methods.
Output: Design tokens in globals.css, AuthGuard component, extended auth-store, extended api.ts.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-collaboration-prod/06-CONTEXT.md
@.planning/phases/06-collaboration-prod/06-UI-SPEC.md

<interfaces>
From web/src/stores/auth-store.ts (current):
```typescript
interface User { id: string; email: string; nickname: string; avatar?: string; is_admin: boolean; }
interface AuthState {
  user: User | null; accessToken: string | null; refreshToken: string | null; isAuthenticated: boolean;
  setAuth: (user: User, accessToken: string, refreshToken: string) => void; logout: () => void;
}
```

From web/src/lib/api.ts (current):
```typescript
export const authApi = { register, login, me, refresh }
export const skillsApi = { list, tools, invoke, poll }
export const canvasApi = { list, get, create, update, delete, ... }
export const agentApi = { createSession, listSessions, ... }
export const billingApi = { usageStats, usageTimeseries, pricing }
```

From web/src/components/providers.tsx (current):
```typescript
export function Providers({ children }: { children: ReactNode }) {
  return <QueryClientProvider client={queryClient}>{children}</QueryClientProvider>;
}
```

From web/src/app/layout.tsx (current):
```typescript
import { Geist, Geist_Mono } from "next/font/google";
// Uses geistSans and geistMono font variables
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design tokens + AuthGuard + fonts + Providers</name>
  <read_first>
    - web/src/app/globals.css
    - web/src/app/layout.tsx
    - web/src/components/providers.tsx
  </read_first>
  <files>web/src/app/globals.css, web/src/app/layout.tsx, web/src/components/auth/auth-guard.tsx, web/src/components/providers.tsx</files>
  <action>
**1. Append Obsidian Lens tokens to `web/src/app/globals.css`** (per UI-SPEC §2) — add AFTER the existing Phase 05 `.react-flow__node.selected` block, BEFORE the `.cv4-md-editor-preview` section:

```css
/* === Phase 06 Obsidian Lens Design Tokens === */
:root, .theme-dark {
  --ob-surface-base: #0E0E0E;
  --ob-surface-lowest: #080808;
  --ob-surface-low: #111111;
  --ob-surface-mid: #141414;
  --ob-surface-high: #1A1A1A;
  --ob-surface-highest: #222222;
  --ob-surface-bright: #2A2A2A;

  --ob-primary: #00D1FF;
  --ob-primary-dark: #0087A3;
  --ob-primary-glow: rgba(0,209,255,0.3);
  --ob-tertiary: #FFD59C;
  --ob-error: #FFB4AB;
  --ob-success: #4CAF50;

  --ob-text-primary: #E5E2E1;
  --ob-text-secondary: #BBC9CF;
  --ob-text-muted: #8E9192;
  --ob-text-on-primary: #000000;

  --ob-glass-bg: rgba(18,18,18,0.8);
  --ob-glass-border: rgba(255,255,255,0.08);
  --ob-glass-hover: rgba(22,22,22,0.9);
  --ob-glass-hover-border: rgba(0,209,255,0.3);

  --ob-glow-sm: 0 0 8px #00D1FF;
  --ob-glow-lg: 0 0 32px rgba(0,209,255,0.3);
  --ob-glow-card: 0 0 40px -10px rgba(0,209,255,0.15);

  --ob-gradient-primary: linear-gradient(135deg, #00D1FF 0%, #0087A3 100%);
  --ob-dot-grid: radial-gradient(rgba(255,255,255,0.05) 1px, transparent 0);
  --ob-dot-grid-size: 24px 24px;
}
```

**2. Update `web/src/app/layout.tsx`** — replace Geist fonts with Space Grotesk + Manrope:

```typescript
import { Space_Grotesk, Manrope } from "next/font/google";

const spaceGrotesk = Space_Grotesk({
  variable: "--font-headline",
  subsets: ["latin"],
  weight: ["500", "700"],
});

const manrope = Manrope({
  variable: "--font-body",
  subsets: ["latin"],
  weight: ["400", "500", "600"],
});
```

Update the `<html>` className to `{spaceGrotesk.variable} ${manrope.variable} h-full antialiased dark`.
Update the `<body>` className to `min-h-full flex flex-col` (remove the gray bg/text classes — Obsidian Lens uses CSS custom props). Add `style={{ background: 'var(--ob-surface-base)', color: 'var(--ob-text-primary)' }}` to body.

**3. Create `web/src/components/auth/auth-guard.tsx`** (per D-25 + research Pattern 4):

```tsx
"use client";
import { useEffect, useState } from "react";
import { useRouter, usePathname } from "next/navigation";
import { useAuthStore } from "@/stores/auth-store";

const PUBLIC_PATHS = ["/login", "/register", "/invite"];

export function AuthGuard({ children }: { children: React.ReactNode }) {
  const { isAuthenticated } = useAuthStore();
  const router = useRouter();
  const pathname = usePathname();
  const [mounted, setMounted] = useState(false);

  useEffect(() => setMounted(true), []);

  useEffect(() => {
    if (mounted && !isAuthenticated && !PUBLIC_PATHS.some(p => pathname?.startsWith(p))) {
      router.replace("/login");
    }
  }, [mounted, isAuthenticated, pathname, router]);

  if (!mounted) return null;
  if (!isAuthenticated && !PUBLIC_PATHS.some(p => pathname?.startsWith(p))) return null;

  return <>{children}</>;
}
```

**4. Update `web/src/components/providers.tsx`** — wrap children with AuthGuard:

```tsx
import { AuthGuard } from "@/components/auth/auth-guard";

export function Providers({ children }: { children: ReactNode }) {
  const [queryClient] = useState(() => new QueryClient({ ... }));
  return (
    <QueryClientProvider client={queryClient}>
      <AuthGuard>{children}</AuthGuard>
    </QueryClientProvider>
  );
}
```
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - web/src/app/globals.css contains `--ob-primary: #00D1FF`
    - web/src/app/globals.css contains `--ob-glass-bg:`
    - web/src/app/globals.css contains `--ob-dot-grid:`
    - web/src/app/layout.tsx contains `Space_Grotesk`
    - web/src/app/layout.tsx contains `Manrope`
    - web/src/app/layout.tsx contains `--font-headline`
    - web/src/components/auth/auth-guard.tsx contains `export function AuthGuard`
    - web/src/components/auth/auth-guard.tsx contains `PUBLIC_PATHS`
    - web/src/components/providers.tsx contains `AuthGuard`
  </acceptance_criteria>
  <done>Obsidian Lens CSS tokens active. AuthGuard protects all routes. Fonts loaded. Providers wraps AuthGuard.</done>
</task>

<task type="auto">
  <name>Task 2: Auth store extension + API client extension</name>
  <read_first>
    - web/src/stores/auth-store.ts
    - web/src/lib/api.ts
  </read_first>
  <files>web/src/stores/auth-store.ts, web/src/lib/api.ts</files>
  <action>
**1. Extend `web/src/stores/auth-store.ts`** (per D-20 + research Pattern 5):

Add interfaces:
```typescript
interface Team {
  id: string;
  name: string;
  description?: string;
  avatar?: string;
  my_role: string;
}

type SpaceContext =
  | { type: "personal" }
  | { type: "team"; teamId: string; teamName: string };
```

Extend `AuthState` interface with:
```typescript
  teams: Team[];
  currentSpace: SpaceContext;
  setTeams: (teams: Team[]) => void;
  switchSpace: (space: SpaceContext) => void;
```

In the store implementation, add:
- `teams: []` initial state
- `currentSpace: { type: "personal" }` initial state
- `setTeams: (teams) => set({ teams })` action
- `switchSpace: (space) => set({ currentSpace: space })` action
- In `logout`, reset `teams: []` and `currentSpace: { type: "personal" }`

**2. Extend `web/src/lib/api.ts`** with new API namespaces:

```typescript
export const teamsApi = {
  list: () => api.get("/teams/"),
  get: (teamId: string) => api.get(`/teams/${teamId}`),
  create: (data: { name: string; description?: string }) => api.post("/teams/", data),
  update: (teamId: string, data: { name?: string; description?: string }) => api.patch(`/teams/${teamId}`, data),
  delete: (teamId: string) => api.delete(`/teams/${teamId}`),
  listMembers: (teamId: string) => api.get(`/teams/${teamId}/members`),
  addMember: (teamId: string, data: { user_id: string; role?: string }) => api.post(`/teams/${teamId}/members`, data),
  updateMember: (teamId: string, memberId: string, data: { role: string }) => api.patch(`/teams/${teamId}/members/${memberId}`, data),
  removeMember: (teamId: string, memberId: string) => api.delete(`/teams/${teamId}/members/${memberId}`),
  createInvitation: (teamId: string, data: { role?: string }) => api.post(`/teams/${teamId}/invitations`, data),
  acceptInvitation: (token: string) => api.post(`/teams/invitations/${token}/accept`),
  listGroups: (teamId: string) => api.get(`/teams/${teamId}/groups`),
  createGroup: (teamId: string, data: { name: string; description?: string }) => api.post(`/teams/${teamId}/groups`, data),
  updateGroup: (teamId: string, groupId: string, data: { name?: string; description?: string }) => api.patch(`/teams/${teamId}/groups/${groupId}`, data),
  deleteGroup: (teamId: string, groupId: string) => api.delete(`/teams/${teamId}/groups/${groupId}`),
  listGroupMembers: (teamId: string, groupId: string) => api.get(`/teams/${teamId}/groups/${groupId}/members`),
  addGroupMember: (teamId: string, groupId: string, data: { user_id: string; role?: string }) => api.post(`/teams/${teamId}/groups/${groupId}/members`, data),
  updateGroupMember: (teamId: string, groupId: string, memberId: string, data: { role: string }) => api.patch(`/teams/${teamId}/groups/${groupId}/members/${memberId}`, data),
  removeGroupMember: (teamId: string, groupId: string, memberId: string) => api.delete(`/teams/${teamId}/groups/${groupId}/members/${memberId}`),
};

export const projectsApi = {
  list: (params?: { owner_type?: string; owner_id?: string }) => api.get("/projects/", { params }),
  get: (projectId: string) => api.get(`/projects/${projectId}`),
  create: (data: { name: string; description?: string; owner_type: string; owner_id: string }) => api.post("/projects/", data),
  update: (projectId: string, data: { name?: string; description?: string }) => api.patch(`/projects/${projectId}`, data),
  delete: (projectId: string) => api.delete(`/projects/${projectId}`),
  clone: (projectId: string, data: { target_owner_type: string; target_owner_id: string }) => api.post(`/projects/${projectId}/clone`, data),
};

export const usersApi = {
  search: (q: string) => api.get("/users/search", { params: { q } }),
  profile: () => api.get("/users/me"),
  updateProfile: (data: { nickname?: string; avatar?: string }) => api.patch("/users/me", data),
};

export const aiProvidersApi = {
  list: (params?: { owner_type?: string; owner_id?: string }) => api.get("/ai-providers/", { params }),
  get: (providerId: string) => api.get(`/ai-providers/${providerId}`),
  create: (data: { provider_name: string; display_name: string; owner_type: string; owner_id?: string }) => api.post("/ai-providers/", data),
  update: (providerId: string, data: Record<string, unknown>) => api.patch(`/ai-providers/${providerId}`, data),
  delete: (providerId: string) => api.delete(`/ai-providers/${providerId}`),
  addKey: (providerId: string, data: { api_key: string; label?: string }) => api.post(`/ai-providers/${providerId}/keys`, data),
  deleteKey: (providerId: string, keyId: string) => api.delete(`/ai-providers/${providerId}/keys/${keyId}`),
  listModels: (params?: { model_type?: string }) => api.get("/ai-models/", { params }),
};
```

Also extend `authApi` with OAuth methods:
```typescript
  googleLogin: () => api.get("/auth/oauth/google/login"),
  githubLogin: () => api.get("/auth/oauth/github/login"),
```
  </action>
  <verify>
    <automated>cd web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - web/src/stores/auth-store.ts contains `interface Team`
    - web/src/stores/auth-store.ts contains `type SpaceContext`
    - web/src/stores/auth-store.ts contains `currentSpace`
    - web/src/stores/auth-store.ts contains `switchSpace`
    - web/src/stores/auth-store.ts contains `setTeams`
    - web/src/lib/api.ts contains `export const teamsApi`
    - web/src/lib/api.ts contains `export const projectsApi`
    - web/src/lib/api.ts contains `export const usersApi`
    - web/src/lib/api.ts contains `export const aiProvidersApi`
    - web/src/lib/api.ts contains `googleLogin`
    - web/src/lib/api.ts contains `githubLogin`
  </acceptance_criteria>
  <done>Auth store supports team/space switching. API client covers all Phase 06 backend endpoints.</done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- AuthGuard redirects unauthenticated users
- Auth store persists teams and currentSpace
- All API client methods reference correct endpoint paths
</verification>

<success_criteria>
- Obsidian Lens tokens available as CSS custom properties
- AuthGuard protects all non-public routes
- Auth store tracks team membership and current space
- API client ready for all Phase 06 features
</success_criteria>

<output>
After completion, create `.planning/phases/06-collaboration-prod/06-03-SUMMARY.md`
</output>
