# Phase 03.1: Agent Chat + Canvas Quality Fix - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-03-29
**Phase:** 03.1-agent-chat-canvas-quality-fix
**Areas discussed:** Agent 上下文注入, Canvas 节点数据流, 节点持久化时机, 整图执行边界, Agent 思考范式

---

## Agent 上下文注入

| Option | Description | Selected |
|--------|-------------|----------|
| 基础信息 | 只注入项目名称 + 画布名称 | |
| 结构化摘要 | 项目名 + 节点摘要 + 角色/场景列表名称（200-500 tokens） | |
| 完整上下文 | 项目详情 + 全部节点数据 + 角色/场景/剧本内容 | ✓ |

**User's choice:** 完整上下文
**Notes:** 用户希望 Agent 拥有项目全貌

### 追问：Token 超限处理

| Option | Description | Selected |
|--------|-------------|----------|
| 智能截断 | 8K token 预算，超出自动摘要压缩 | |
| 全量注入 | 信任模型长上下文能力，不截断 | |
| 按需加载 | 先注入结构摘要，Agent 通过工具调用获取详情 | ✓ |

**User's choice:** 按需加载
**Notes:** 兼顾全局视野和 token 成本控制

---

## Canvas 节点数据流

### 数据类型

| Option | Description | Selected |
|--------|-------------|----------|
| 只传 text | 所有节点间只传文本 | |
| 类型化数据流 | 区分 text/json/imageUrl，连线校验类型兼容性 | ✓ |

**User's choice:** 类型化数据流，每个节点依据功能管理好输入和输出的类型

### 多上游输入处理

| Option | Description | Selected |
|--------|-------------|----------|
| 拼接合并 | 多个 text 拼接，多个 imageUrl 取第一个 | |
| 命名端口 | 节点多输入端口，用户连线选端口 | |
| 优先级取一 | 多个同类型只取第一个 | |
| 数组聚合 | 同类型聚合为数组（用户提出） | ✓ |

**User's choice:** 支持多上下游，同类型用该类型的数组
**Notes:** 用户主动提出数组聚合方案，节点内部自行决定消费方式

---

## 节点持久化时机

| Option | Description | Selected |
|--------|-------------|----------|
| 防抖自动保存 | 2s 无操作自动保存 | |
| Blur 保存 | 失焦时保存 | |
| 显式保存 | 保存按钮 / Ctrl+S | |
| 混合策略 | 防抖 + blur + 关键操作立即保存 | ✓ |

**User's choice:** 混合 — 防抖自动保存 + blur 保存 + 关键操作立即保存
**Notes:** 三层保障

---

## 整图执行边界

| Option | Description | Selected |
|--------|-------------|----------|
| 仅修前端数据流 | 节点间能通过边传数据，手动单节点执行 | ✓ |
| 前端顺序执行 | 前端按拓扑排序依次自动触发 | |
| 后端拓扑执行 | 新增整图执行 API + SSE 进度 | |

**User's choice:** 仅修前端数据流
**Notes:** 与 Phase 03 D3 一致，Graph-Based 留 Phase 05

---

## Agent 思考范式

| Option | Description | Selected |
|--------|-------------|----------|
| 被动执行 | 用户明确指令才执行 | |
| 主动规划 | 分析意图后输出执行计划 | |
| 分级响应 | 简单请求直接执行，复杂请求先规划再确认 | ✓ |

**User's choice:** 分级响应

### 追问：语言风格

| Option | Description | Selected |
|--------|-------------|----------|
| 专业简洁 | 工具结果直接展示，回复简短 | |
| 导演助手风格 | 结合创作术语，带建议 | |
| Claude 自行把握 | 不做硬性约束 | ✓ |

**User's choice:** 目前暂时不设定
**Notes:** 交由模型自行把握

---

## Claude's Discretion

- Agent 语言风格和回复详略程度
- 防抖保存具体实现细节
- useUpstreamData hook API 设计
- 上下文查询工具返回格式

## Deferred Ideas

- Graph-Based 整图执行 → Phase 05
- Agent 语言风格定制化 → 后续迭代
- 命名端口 → 如果数组聚合不够用再考虑
- 节点执行结果缓存/版本化 → Phase 05
